{
  "scripts": {
    "test": "npm --prefix functions/backend ci && npm --prefix enable-device ci && npm run test --prefix functions/backend && npm run test --prefix enable-device"
  },
  "dependencies": {
    "@firebase/auth": "^1.12.0"
  }
}
