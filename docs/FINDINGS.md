# Findings

Elenco progressivo di bug preesistenti, comportamenti anomali o ambiguita' non documentate scoperte durante le sessioni di deep-dive del cognitive workspace. Non correggere il comportamento segnalato in questo file di propria iniziativa: la correzione richiede decisione umana esplicita, salvo diversa indicazione.

## F-1: `deleteChecklist` verifica un campo `createdBy` che nessuna funzione del core Organizer scrive mai

- **Descrizione**: `deleteChecklist` consente l'eliminazione di una checklist se `role === 'admin'` oppure se `data.createdBy === uid` (il chiamante e' il "creatore" della checklist). Tuttavia nessuna delle funzioni di creazione del core Organizer (`createChecklist`, `createChecklistFromTemplate`, `cloneChecklist`) scrive mai il campo `createdBy` sul documento `checklists/{id}` al momento della creazione. Di conseguenza, in produzione `data.createdBy` e' sempre `undefined` per qualunque checklist creata da questo modulo, e non potra' mai coincidere con un `uid` autenticato valido: il ramo "il creatore puo' eliminare la propria checklist" e' codice morto, e nella pratica **solo un admin puo' eliminare una checklist**, indipendentemente da chi l'abbia creata.
- **Evidenza**:
  - `functions/backend/src/organizer/deleteChecklist.ts:39` — `if (role !== "admin" && data.createdBy !== uid)`
  - `functions/backend/src/organizer/createChecklist.ts:123-128` — `checklistRef.set({...})` senza `createdBy`
  - `functions/backend/src/organizer/createChecklistFromTemplate.ts:124-130` — idem
  - `functions/backend/src/organizer/cloneChecklist.ts:133-139` — idem
  - `functions/backend/src/organizer/deleteChecklist.test.ts:71` — il test imposta `createdBy` manualmente sul mock del documento, mascherando l'assenza del campo nel codice di produzione
- **Story/PR/sessione di provenienza**: Epic Jira EA-3 "Organizer Core" (riferimento da `docs/cognitive-workspace/discover-context.json`, non verificabile via API Jira in questa sessione per permessi mancanti); rilevato durante deep-dive del dominio `process-organizer-core`, sessione 2026-07-28.
- **Stato**: risolto (Task Jira EA-120, 2026-07-28). `createChecklist.ts`, `createChecklistFromTemplate.ts` e `cloneChecklist.ts` ora scrivono `createdBy: uid` sul documento `checklists/{id}` al momento della creazione. `deleteChecklist.test.ts` non imposta più `createdBy` a mano sul mock: verifica il ramo "creatore" creando la checklist tramite `createChecklist.run(...)` e controllando che `deleteChecklist` legga davvero il valore scritto da quest'ultima.

## F-2: Copertura incoerente di `logSecurityEvent` tra le Cloud Function del core Organizer

- **Descrizione**: la convenzione di repository documentata ("Ogni Cloud Function deve loggare inizio, fine ed errori in securityLogs via `logSecurityEvent`", vedi `docs/cognitive-workspace/discover-context.json` -> `conventions.security_logging`) non e' applicata uniformemente nel modulo Organizer. `updateChecklist`, `deleteChecklist`, `addChecklistItem`, `updateChecklistItem`, `removeChecklistItem` chiamano `logSecurityEvent` sia su successo sia su fallimento; `createChecklist`, `createChecklistFromTemplate`, `cloneChecklist`, `getChecklist`, `listTemplates`, `createTemplate`, `updateTemplate`, `deleteTemplate`, `getChecklistCompleteness` non lo fanno mai (usano solo `console.log`/`console.error`).
- **Evidenza**:
  - Con `logSecurityEvent`: `functions/backend/src/organizer/updateChecklist.ts`, `deleteChecklist.ts`, `addChecklistItem.ts`, `updateChecklistItem.ts`, `removeChecklistItem.ts`
  - Senza `logSecurityEvent`: `functions/backend/src/organizer/createChecklist.ts`, `createChecklistFromTemplate.ts`, `cloneChecklist.ts`, `getChecklist.ts`, `listTemplates.ts`, `createTemplate.ts`, `updateTemplate.ts`, `deleteTemplate.ts`, `getChecklistCompleteness.ts`
- **Story/PR/sessione di provenienza**: Epic Jira EA-3 "Organizer Core"; rilevato durante deep-dive del dominio `process-organizer-core`, sessione 2026-07-28.
- **Stato**: risolto (Task Jira EA-120, 2026-07-28). Le 9 funzioni prive di copertura ora chiamano `logSecurityEvent` sia su successo sia su fallimento, con lo stesso pattern di `updateChecklist.ts`/`deleteChecklist.ts` (azione `<verbo>_<risorsa>` in caso di successo, `<verbo>_<risorsa>_failed` in caso di fallimento). `getChecklist.ts` e `getChecklistCompleteness.ts` non avevano un blocco `try/catch`: è stato introdotto per poter loggare l'esito, senza cambiare il comportamento delle validazioni esistenti.

## F-3: `createTemplate.ts` era privo di test automatici prima di EA-120

- **Descrizione**: a differenza delle altre Cloud Function del core Organizer, `createTemplate.ts` non aveva un file `createTemplate.test.ts`. Non è chiaro se si tratti di una dimenticanza o di una scelta deliberata non documentata.
- **Evidenza**: assenza di `functions/backend/src/organizer/createTemplate.test.ts` prima di questa Story (verificato con `ls functions/backend/src/organizer/*.test.ts`).
- **Story/PR/sessione di provenienza**: rilevato durante l'implementazione del Task Jira EA-120 (F-2), sessione 2026-07-28. Un file di test minimo è stato aggiunto in questa stessa Story per coprire il nuovo comportamento `logSecurityEvent`, ma non è stata condotta una revisione sistematica della copertura di test pre-esistente del modulo Organizer.
- **Stato**: risolto (audit svolto, 2026-08-12, su richiesta esplicita dell'operatore). Verificato con `ls functions/backend/src/organizer/*.ts`: tutti i 19 file sorgente del modulo hanno oggi un `.test.ts` corrispondente (nessuno mancante), con copertura misurata (`jest --coverage`) di 94.98% statement / 100% function / 85.82% branch — il gap originale (assenza di `createTemplate.test.ts`) è stato colmato dal lavoro delle Story successive (EA-12x/EA-13x). Nessuna azione ulteriore necessaria.

## F-4: Il dropdown di stato item in `ChecklistPanel.tsx` non è mai disabilitato, in contraddizione con quanto riportato nella richiesta `ss-checklist-item-model`

- **Descrizione**: la richiesta di implementazione (validazione su staging `enableitalia-staging`) afferma che "lo stato dell'item sembra bloccato su 'Assegnare' ... e non modificabile, sia quando l'assegnatario è valorizzato sia quando non lo è". La lettura del codice attuale non conferma questo comportamento: la colonna "Stato" di `ChecklistPanel.tsx` usa un `Dropdown` PrimeReact senza alcuna prop `disabled`, sempre editabile indipendentemente dal valore di `assignee`. La cronologia git del file (dall'introduzione in EA-111 fino a EA-113) non mostra mai l'aggiunta di una prop `disabled` su questo componente.
- **Evidenza**:
  - `enable-device/src/components/checklist/ChecklistPanel.tsx` — colonna "Stato", componente `Dropdown` privo di `disabled`
  - `git log -p --follow -- enable-device/src/components/checklist/ChecklistPanel.tsx | grep disabled` — le uniche occorrenze di `disabled` riguardano i pulsanti "Salva" (`disabled={!isDirty(item.id)}`) e "Aggiungi" (`disabled={!newItem.title.trim()}`), mai il Dropdown di stato
- **Story/PR/sessione di provenienza**: rilevato durante il grounding dello studio di soluzione `ss-checklist-item-model`, sessione 2026-07-30.
- **Stato**: chiuso, falso positivo (confermato dall'operatore, 2026-08-12). L'osservazione in staging non corrisponde a un blocco effettivo del controllo nel codice attuale; nessuna azione necessaria.

## F-5: Il campo `quantity` viene scritto incondizionatamente da tutte le funzioni di creazione/clonazione item del core Organizer: il ramo "quantity irrilevante" del gate di completezza non è mai esercitato in produzione

- **Descrizione**: `checklistCompleteness.ts` (`hasQuantityWhenRelevant`) è progettato per trattare un item come "senza vincolo di quantità" quando la chiave `quantity` è del tutto assente dall'oggetto, distinguendola esplicitamente dal caso "chiave presente con valore `null`" (che invece richiede valorizzazione). Nella pratica, però, **tutte** le funzioni che producono un item (`createChecklist.normalizeInitialItem`, `addChecklistItem`, `createChecklistFromTemplate`, `cloneChecklist.cloneSourceItem`) scrivono sempre la chiave `quantity` (con `?? null` come fallback), senza mai ometterla. Di conseguenza, in produzione ogni item ha sempre la chiave `quantity` presente, e il ramo "quantity irrilevante" del gate non si attiva mai: qualunque item, indipendentemente dalla sua natura (attività vs materiale), è oggi trattato dal gate come se la quantità fosse sempre rilevante.
- **Evidenza**:
  - `functions/backend/src/organizer/createChecklist.ts:71` — `quantity: (quantity as number | undefined) ?? null` (sempre presente)
  - `functions/backend/src/organizer/addChecklistItem.ts:79` — `quantity: quantity ?? null`
  - `functions/backend/src/organizer/createChecklistFromTemplate.ts:48` — `quantity` sempre presente
  - `functions/backend/src/organizer/cloneChecklist.ts:44-52` — `quantity` sempre presente
  - `functions/backend/src/organizer/checklistCompleteness.ts` — `hasQuantityWhenRelevant` distingue esplicitamente chiave-assente da chiave-null, ramo mai raggiunto dai produttori di item sopra elencati
- **Story/PR/sessione di provenienza**: rilevato durante il grounding dello studio di soluzione `ss-checklist-item-model`, sessione 2026-07-30. Rilevante per lo studio stesso: qualunque opzione che assuma "un item può oggi non avere quantità" come stato raggiungibile deve prima correggere queste 4 funzioni perché omettano davvero la chiave quando non fornita.
- **Stato**: risolto come effetto collaterale di EA-127 (sessione 2026-08-05). Il gate di completezza type-aware (`checklistCompleteness.ts::hasQuantity`, che ha sostituito `hasQuantityWhenRelevant`) controlla `quantity` solo per item `numeric` e tratta chiave-assente e chiave-`null` in modo identico: la distinzione che questo finding segnalava non ha più alcun effetto osservabile sul gate. Confermato dall'operatore: nessun fix separato necessario.

## F-6: Lo shorthand a stringa per gli item iniziali di `createChecklist` diventa irraggiungibile dopo l'introduzione del `type` obbligatorio (EA-123)

- **Descrizione**: `normalizeInitialItem` accetta storicamente un item iniziale espresso come semplice stringa (il solo `title`), oltre che come oggetto `{ title, quantity?, notes? }`. Con EA-123 il campo `type` diventa obbligatorio e validato tramite `isChecklistItemType`, ma lo shorthand a stringa non ha alcun modo di veicolare un `type`: di conseguenza qualunque item iniziale passato come stringa fallisce sempre con `invalid-argument` ("Each item must have a valid type..."), rendendo il ramo `typeof input === "string"` di fatto codice morto per qualunque consumer reale (nessuna checklist può più essere creata con item-stringa). Non è stato deciso se questo ramo debba essere rimosso esplicitamente o mantenuto come guardia difensiva: la Story EA-123 non menziona lo shorthand a stringa, quindi non è stato rimosso in questa sessione per restare aderenti al piano.
- **Evidenza**:
  - `functions/backend/src/organizer/createChecklist.ts` — branch `typeof input === "string"` in `normalizeInitialItem`: `type` resta `undefined`, quindi `isChecklistItemType(type)` è sempre `false` per questo branch
  - `functions/backend/src/organizer/createChecklist.test.ts` — test "throws invalid-argument when an initial item is a bare string (no type)" verifica esplicitamente che questo path sia sempre rifiutato
- **Story/PR/sessione di provenienza**: rilevato durante l'implementazione della Story Jira EA-123 (Epic checklist item model), sessione 2026-07-31.
- **Stato**: risolto (Story EA-143, sessione 2026-08-05) — rimosso il ramo `typeof input === "string"` da `normalizeInitialItem`; l'input stringa cade ora nel ramo `else` esistente (`invalid-argument`: "Each item must be a string or an object with a title").

## F-7: Lo stesso shorthand a stringa diventa irraggiungibile anche in `createTemplate`/`updateTemplate` dopo l'introduzione del `type` obbligatorio (EA-125)

- **Descrizione**: analogamente a [[F-6]], `normalizeTemplateItem` (duplicata identicamente in `createTemplate.ts` e `updateTemplate.ts`) accetta storicamente un item di template espresso come semplice stringa (il solo `title`), oltre che come oggetto `{ title, quantity? }`. Con EA-125 il campo `type` diventa obbligatorio e validato tramite `isChecklistItemType`, ma lo shorthand a stringa non ha modo di veicolare un `type`: qualunque item di template passato come stringa fallisce sempre con `invalid-argument`, rendendo il ramo `typeof input === "string"` codice morto per qualunque consumer reale. Coerentemente con la decisione presa per F-6 (non rimuovere senza indicazione esplicita nella Story), il ramo è stato preservato as-is anche qui.
- **Evidenza**:
  - `functions/backend/src/organizer/createTemplate.ts` — branch `typeof input === "string"` in `normalizeTemplateItem`: `type` resta `undefined`, quindi `isChecklistItemType(type)` è sempre `false` per questo branch
  - `functions/backend/src/organizer/updateTemplate.ts` — stesso branch, stessa duplicazione della funzione già presente prima di questa Story
  - `functions/backend/src/organizer/createTemplate.test.ts` e `updateTemplate.test.ts` — test "throws invalid-argument when an item is a bare string (no type)" verificano esplicitamente che questo path sia sempre rifiutato
- **Story/PR/sessione di provenienza**: rilevato durante l'implementazione della Story Jira EA-125 (Epic checklist item model), sessione 2026-08-03.
- **Stato**: risolto dalla Story Jira EA-144 (Epic EA-135), sessione 2026-08-05. Il ramo `typeof input === "string"` è stato rimosso da `normalizeTemplateItem`; un item-stringa cade ora nel ramo `else` esistente, rifiutato con `invalid-argument: "Each item must be a string or an object with a title"` invece del precedente (sempre vero) "must have a valid type". Colto anche il suggerimento non vincolante di EA-144 sulla duplicazione (vedi anche [[F-8]]): `normalizeTemplateItem`/`TemplateItem` sono state estratte nel modulo condiviso `functions/backend/src/organizer/templateItem.ts`, riusato identicamente da `createTemplate.ts` e `updateTemplate.ts`, eliminando la duplicazione.

## F-8: `AdminChecklistTemplatesPage.tsx` non inviava mai `type` per gli item di template (EA-125), stesso pattern di [[F-7 EA-124]]

- **Descrizione**: analogamente a quanto già successo con `ChecklistPanel.tsx`/`addDeviceRequestChecklistItem` (EA-124), l'unico consumer frontend reale di `createTemplate`/`updateTemplate` (`AdminChecklistTemplatesPage.tsx`, dialog di creazione/modifica template) non inviava mai un campo `type` per gli item: l'interfaccia `TemplateItemForm` non lo prevedeva, e il payload verso `createDeviceChecklistTemplate`/`updateDeviceChecklistTemplate` veniva ricostruito esplicitamente come `{ title, quantity }`, perdendo `type` anche a form aggiornato. Dopo EA-125, ogni creazione/modifica reale di template dall'interfaccia admin avrebbe fallito sempre con `invalid-argument`.
- **Evidenza**:
  - `enable-device/src/pages/admin/AdminChecklistTemplatesPage.tsx` — `TemplateItemForm`/`EMPTY_ITEM` privi di `type`; payload ricostruito come `{ title: item.title.trim(), quantity: item.quantity }`
  - `functions/backend/src/device-requests/createChecklistTemplate.test.ts` — il test con fixture a stringa (`items: ["Stampa dita"]`) falliva realmente sul merge preview della PR, rivelando il gap
- **Story/PR/sessione di provenienza**: rilevato e risolto in questa stessa PR (EA-125), sessione 2026-08-03.
- **Stato**: risolto in questa PR. Aggiunto `type` a `TemplateItemForm`/`EMPTY_ITEM`, un selettore Dropdown per riga item, e corretta la ricostruzione del payload per includere `type`. Non copre la resa condizionale delle colonne per tipo, che resta scope di EA-128.
- **Seguito (stesso commit di fix)**: il panel review ha trovato un secondo aspetto dello stesso problema — `openEditTemplate` caricava gli item di un template *già esistente* (creato prima di EA-125, quindi privo di `type` in Firestore) senza alcun default, e `saveTemplate` invia sempre l'intero array `items` anche quando si modifica solo il titolo: qualunque modifica a un template legacy sarebbe quindi fallita con `invalid-argument`, con un messaggio d'errore generico che suggerisce (erroneamente) un problema di permessi. Corretto applicando `type: item.type ?? "generic"` in caricamento, coerente con la mitigazione già prevista per i dati storici nello studio `ss-checklist-item-model` (trattare l'assenza di `type` come `"generic"`, comportamento transitorio).

## F-9: `cloneTemplateItem`/`cloneSourceItem` (EA-126) applicano un default `"generic"` per item sorgente privi di `type`, comportamento non coperto dagli scenari Gherkin della Story

- **Descrizione**: gli scenari Gherkin di EA-126 assumono sempre "un template/istanza con un item che ha `type` valorizzato", coerentemente con l'invariante stabilita da EA-123/EA-125 ("ogni item di istanza e di template ha sempre il campo `type` valorizzato"). L'invariante vale però solo per gli item creati/modificati **dopo** l'introduzione della validazione obbligatoria in EA-123 (istanze) ed EA-125 (template): i documenti Firestore già esistenti prima di quelle Story (checklist/template creati o mai più modificati da allora) possono avere item privi del campo `type`. Se `cloneTemplateItem`/`cloneSourceItem` propagassero `type` senza alcuna guardia, un item legacy produrrebbe un item clonato con `type: undefined`, violando l'invariante sulla nuova istanza (a valle non validata da nessun controllo, essendo una scrittura diretta e non un `normalizeInitialItem`). È stato quindi applicato lo stesso default transitorio `"generic"` già usato in [[F-8]] per `AdminChecklistTemplatesPage.tsx`, invece di propagare `undefined` o rifiutare la clonazione con un errore.
- **Evidenza**:
  - `functions/backend/src/organizer/createChecklistFromTemplate.ts` — `cloneTemplateItem`: `isChecklistItemType(templateItem?.type) ? templateItem.type : "generic"`
  - `functions/backend/src/organizer/cloneChecklist.ts` — `cloneSourceItem`: stesso pattern
  - `functions/backend/src/organizer/createChecklistFromTemplate.test.ts` e `cloneChecklist.test.ts` — test "defaults type to 'generic' when a legacy ... item has no type"
- **Story/PR/sessione di provenienza**: rilevato e risolto (con scelta di default, non da Story esplicita) durante l'implementazione della Story Jira EA-126, sessione 2026-08-03.
- **Stato**: confermato dall'operatore (sessione 2026-08-05). Il default `"generic"` per item sorgente legacy privi di `type` resta il comportamento voluto, coerente con [[F-10]]. Nessuna azione di codice.

## F-10: Il gate di completezza type-aware (EA-127) tratta gli item privi di `type` come 'generic': scelta non coperta dagli scenari Gherkin della Story

- **Descrizione**: EA-127 richiede che `isChecklistItemComplete` distingua il calcolo per `type` ('boolean' | 'generic' | 'numeric'), ma nessuno dei 6 scenari di accettazione copre il caso in cui `type` sia assente o non riconosciuto — situazione reale per qualunque item creato prima di EA-123 (che ha reso `type` obbligatorio solo per le nuove creazioni, senza backfill dei documenti Firestore esistenti) e per i fixture di test pre-esistenti di `getChecklistCompleteness.test.ts` e `getDeviceRequestChecklistCompleteness.test.ts` che non impostavano `type`. In questa Story si è scelto, senza conferma umana esplicita, di far ricadere `type` assente o non riconosciuto sul calcolo `generic` (assegnatario valorizzato + `status === 'Completata'`), l'unico esprimibile con i soli campi storicamente disponibili (`status`, senza `completed`). Stessa scelta di default già presa in [[F-9]] per la clonazione — coerente tra le due Story.
- **Evidenza**:
  - `functions/backend/src/organizer/checklistCompleteness.ts` — `isChecklistItemComplete`: `if (item.type === "boolean") ... if (item.type === "numeric") ... return isGenericItemComplete(item)` (fallback implicito su `generic` per qualunque altro valore, incluso `undefined`)
  - `functions/backend/src/organizer/checklistCompleteness.test.ts` — describe "type assente o non riconosciuto (item legacy pre-EA-123)"
- **Story/PR/sessione di provenienza**: Story Jira EA-127 "Checklist item: il gate di completezza distingue il calcolo per `type`...", sessione 2026-08-03.
- **Stato**: confermato dall'operatore (sessione 2026-08-05), coerente con [[F-9]]. Il fallback a `generic` per `type` assente/non riconosciuto resta il comportamento voluto; nessun backfill deciso per ora. Nessuna azione di codice.

## F-11: Il ramo `boolean` del gate di completezza type-aware (EA-127) è irraggiungibile in produzione: nessuna funzione scrive mai `completed: true`

- **Descrizione**: EA-127 introduce `isBooleanItemComplete` (completo se `assignee` valorizzato e `completed === true`), corretto e testato come funzione pura, ma nessuna Cloud Function in tutto il repo — né nel core Organizer né nel layer `device-requests` né nel frontend — scrive mai `completed: true` su un item. Le uniche scritture del campo sono `completed: false` in fase di creazione/clonazione (`createChecklist.ts`, `addChecklistItem.ts`, `cloneChecklist.ts`, `createChecklistFromTemplate.ts`); `updateChecklistItem.ts` non accetta `completed` tra i campi aggiornabili. Un item `type='boolean'` può quindi essere creato, ma non esiste oggi alcun percorso applicativo che lo faccia mai risultare completo — stessa classe di problema già documentata in [[F-5]] (un ramo del gate mai esercitato in produzione perché nessuna funzione lo rende raggiungibile).
- **Evidenza**:
  - `functions/backend/src/organizer/checklistCompleteness.ts` — `isBooleanItemComplete` referenzia `item.completed`
  - `grep -rn "completed" functions/backend/src enable-device/src` — nessuna scrittura di `completed: true` in nessun file, in nessun layer
  - `functions/backend/src/organizer/updateChecklistItem.ts` — campi aggiornabili: `title`/`type`/`status`/`assignee`/`quantity`/`notes`, mai `completed`
- **Story/PR/sessione di provenienza**: rilevato durante la review della Story Jira EA-127 (Epic checklist item model), sessione 2026-08-03.
- **Stato**: risolto (Story Jira EA-145, 2026-08-05). `updateChecklistItem.ts` accetta ora `completed: boolean` come campo opzionale aggiornabile (invalid-argument se fornito non booleano, invariato se omesso); `updateDeviceRequestChecklistItem.ts` lo inoltra al core senza introdurre nuova logica RBAC. Il ramo `isBooleanItemComplete` di `checklistCompleteness.ts` (EA-127) e' ora raggiungibile da un percorso applicativo reale; `checklistCompleteness.ts` non e' stato modificato.

## F-12: `type` obbligatorio su `addChecklistItem` (EA-124) rende inutilizzabile in produzione il layer consumer `addDeviceRequestChecklistItem`, che non lo inoltra mai

- **Descrizione**: EA-124 rende `type` obbligatorio (validato con `isChecklistItemType`, nessun default) sulla Cloud Function core `addChecklistItem`, coerentemente con quanto già fatto da EA-123 su `createChecklist`. Il layer di integrazione `device-requests` (`addDeviceRequestChecklistItem.ts`), che è il punto reale da cui il frontend (`ChecklistPanel.tsx`, azione "Aggiungi" item) invoca `addChecklistItem.run(...)` internamente, non riceve né inoltra mai un campo `type`: costruisce il payload passato al core con solo `{ checklistId, title, assignee, quantity, notes }`. Di conseguenza, dopo questa Story, **ogni chiamata reale a `addDeviceRequestChecklistItem` fallirà sempre** con `invalid-argument` ("Each item must have a valid type..."), perché `type` è sempre `undefined` in quel payload. Lo stesso vale, in modo del tutto analogo, per `updateDeviceRequestChecklistItem.ts` rispetto al nuovo campo `type` opzionale di `updateChecklistItem` introdotto da questa stessa Story: qui non si tratta di un fallimento sistematico (il campo resta opzionale), ma nessun consumer del layer device-requests può oggi aggiornare il `type` di un item esistente, perché il campo non è nell'elenco dei parametri accettati da `updateDeviceRequestChecklistItem`. Questa Story non tocca il layer `device-requests` né `ChecklistPanel.tsx` perché il perimetro dichiarato è esplicitamente `process-organizer-core/cap-checklist-item-management` (solo `addChecklistItem`/`updateChecklistItem`), lo stesso perimetro già usato da EA-123 per `createChecklist` — ma in quel caso il consumer (`createDeviceRequestChecklist.ts`) chiama sempre `createChecklist` con `items: []`, quindi `normalizeInitialItem` non viene mai eseguito e la Story non introduce nessuna rottura immediata. Qui invece il breaking change è immediato e reale, non solo un gap di copertura futura.
- **Evidenza**:
  - `functions/backend/src/organizer/addChecklistItem.ts` — `if (!isChecklistItemType(type)) throw new HttpsError("invalid-argument", ...)`, nessun default
  - `functions/backend/src/device-requests/addDeviceRequestChecklistItem.ts:32-50` — destructuring del payload (`requestId, title, assignee, quantity, notes`, nessun `type`) e chiamata `addChecklistItem.run({ ...request, data: { checklistId, title, assignee, quantity, notes } })`
  - `functions/backend/src/device-requests/updateDeviceRequestChecklistItem.ts:33-53` — idem, nessun `type` nel payload inoltrato a `updateChecklistItem.run(...)`
  - `functions/backend/src/organizer/createDeviceRequestChecklist.ts` — per confronto, il caso analogo di EA-123 non rompe nulla perché chiama sempre `createChecklist` con `items: []`
- **Story/PR/sessione di provenienza**: rilevato durante l'implementazione della Story Jira EA-124 (Epic checklist item model), sessione 2026-07-31.
- **Stato**: risolto in questa stessa PR (EA-124). `addDeviceRequestChecklistItem.ts`/`updateDeviceRequestChecklistItem.ts` ora inoltrano `type` al core (commit `a169e48`), e `ChecklistPanel.tsx` espone un selettore `type` (dropdown su `boolean`/`generic`/`numeric`, default `generic`) nel dialog "Aggiungi item", incluso nel payload inviato a `addDeviceRequestChecklistItem`. Non copre l'editing del `type` di un item già esistente (colonne condizionali per tipo restano scope di EA-128).

## F-13: Falso allarme in EA-128 ("`type` non persistito da `addChecklistItem`") causato da EA-124 mancante in `develop` al momento del branch

- **Descrizione**: durante l'implementazione di EA-128 è stato osservato che `addChecklistItem.ts` non leggeva/validava `type`, nonostante EA-124 (Story precedente della stessa Epic, già Done su Jira) avesse esattamente questo scope. La causa non era una regressione: la PR #45 di EA-124 era stata mergiata su `main` invece che su `develop` (aperta prima del cambio di default branch), quindi le sue modifiche non erano mai entrate nella storia di `develop` da cui il branch di EA-128 derivava. Risolto sincronizzando `develop` con `main` (commit `ddc61b6`) prima di chiudere EA-128.
- **Story/PR/sessione di provenienza**: rilevato durante la chiusura della Story Jira EA-128 (Epic checklist item model), sessione 2026-08-03.
- **Stato**: risolto (non era un problema di codice). `addChecklistItem.ts` valida `type` correttamente dopo la sincronizzazione di `develop`; nessuna azione di codice necessaria oltre al sync già effettuato.

## F-14: EA-130 introduce due breaking change lato backend (`checklistId` → `checklistIds`, `sourceChecklistId` obbligatorio) non ancora recepiti dall'unico consumer frontend esistente

- **Descrizione**: EA-130 sostituisce il campo singolare `deviceRequests/{id}.checklistId` con l'array `checklistIds` e rende `sourceChecklistId` un parametro obbligatorio esplicito di `cloneDeviceRequestChecklist`, rimuovendo la risoluzione implicita da `sourceRequestData.checklistId`. Lo scope dichiarato della Story copre solo il percorso di scrittura backend; la UI (`RequestDetail.tsx`, `VolunteerRequestDetail.tsx`, `ChecklistPanel.tsx`) è esplicitamente demandata a una Story separata della stessa Epic ("UI a tab"). L'unico consumer frontend reale oggi esistente, però, non è stato aggiornato in questa Story e continua a: (a) leggere `request.checklistId` (singolare, mai più scritto dopo questa Story) per decidere se mostrare `ChecklistPanel`, e (b) invocare `cloneDeviceRequestChecklist` passando solo `{ requestId, sourceRequestId }`, senza `sourceChecklistId`. Dopo il deploy di questa Story, finché la Story UI a tab non viene implementata: nessuna nuova checklist creata/clonata risulterà mai visibile in `RequestDetail.tsx`/`VolunteerRequestDetail.tsx` (perché `request.checklistId` resta sempre `undefined`), e ogni click su "Clona checklist" da UI fallirà sempre con `invalid-argument` ("Missing or invalid sourceChecklistId"). Coerente con l'indicazione esplicita della Story di non toccare la UI, questo gap non è stato corretto qui.
- **Evidenza**:
  - `enable-device/src/pages/admin/requests/RequestDetail.tsx:263-335` — `createDeviceRequestChecklist`/`cloneDeviceRequestChecklist` invocate senza `sourceChecklistId`; riga 1263 `request.checklistId ? (...) : (...)`
  - `enable-device/src/pages/volunteer/VolunteerRequestDetail.tsx:237-241` — stesso pattern `request.checklistId &&`
  - `enable-device/src/components/checklist/ChecklistPanel.tsx:42,62,97` — prop `checklistId` singolare passata dai due componenti sopra
  - `functions/backend/src/device-requests/createDeviceRequestChecklist.ts` / `cloneDeviceRequestChecklist.ts` (questa Story) — scrivono solo `checklistIds` (arrayUnion), mai più `checklistId`; `cloneDeviceRequestChecklist.ts` richiede `sourceChecklistId` con `invalid-argument` se assente
- **Story/PR/sessione di provenienza**: rilevato durante l'implementazione della Story Jira EA-130 (Epic EA-129), sessione 2026-08-03.
- **Stato**: risolto dalla Story Jira EA-133 ("UI: vista a tab multi-checklist in RequestDetail e VolunteerRequestDetail"), che aggiorna entrambi i consumer a `checklistIds[]` e a `sourceChecklistId` esplicito. Resta com'era la finestra temporale tra il deploy di EA-130 e quello di EA-133 (se distinti), descritta sopra come rischio residuo del deploy sequenziale — non correggibile lato codice.

## F-15: `createChecklistShareLink.ts` è un sesto consumer di `resolveDeviceRequestChecklistAccess`, non elencato tra le "5 Cloud Function del layer" della Story EA-131

- **Descrizione**: la Story EA-131 descrive il cambio di contratto di `resolveDeviceRequestChecklistAccess` (parametro `checklistId` esplicito obbligatorio, verificato per appartenenza a `checklistIds`) come applicato a 5 Cloud Function nominate esplicitamente. In realtà `createChecklistShareLink.ts` (Story EA-113, generazione link di condivisione a sola lettura) chiama lo stesso resolver condiviso e non è mai stata citata nella Story né nei suoi scenari Gherkin. Poiché il cambio di firma del resolver è breaking per definizione, lasciare `createChecklistShareLink.ts` invariata avrebbe rotto la build TypeScript immediatamente (stesso pattern già visto in [[F-12]]): è stata quindi estesa nello stesso modo delle altre 5 (riceve e inoltra `checklistId` esplicito, stesso `invalid-argument` se assente). Questo però estende anche a questa funzione la stessa rottura di contratto verso il frontend descritta in [[F-14]]: `ChecklistPanel.tsx` (`generateShareLink`) chiama oggi `createChecklistShareLink` passando solo `{ requestId }`, quindi fallirà sempre con `invalid-argument` finché il frontend non viene aggiornato per inviare anche `checklistId` — coordinamento frontend+backend non previsto esplicitamente per questa funzione a livello Epic (la nota di rischio in EA-129 cita solo le 5 funzioni nominate).
- **Evidenza**:
  - `functions/backend/src/device-requests/createChecklistShareLink.ts` — unico altro chiamante di `resolveDeviceRequestChecklistAccess` oltre alle 5 funzioni citate dalla Story
  - `enable-device/src/components/checklist/ChecklistPanel.tsx:236-240` — `generateShareLink` invoca `createChecklistShareLink` passando solo `{ requestId }`, nessun `checklistId`
- **Story/PR/sessione di provenienza**: rilevato e risolto (lato backend) durante l'implementazione della Story Jira EA-131 (Epic EA-129), sessione 2026-08-03.
- **Stato**: risolto. Lato backend già in EA-131 (build altrimenti non compilabile). Lato frontend risolto dalla Story Jira EA-133: `ChecklistPanel.tsx` (`generateShareLink`) invia ora `checklistId` esplicito a `createChecklistShareLink`, stesso trattamento delle altre 5 funzioni del layer.

## F-16: Lo scope UI dichiarato dall'Epic EA-129 (solo `RequestDetail.tsx`) non copre `VolunteerRequestDetail.tsx`, che monta lo stesso `ChecklistPanel` singolo e si romperebbe silenziosamente dopo il passaggio a `checklistIds[]`

- **Descrizione**: l'Epic EA-129 descrive il cambio UI solo per `RequestDetail.tsx` ("sostituisce il pannello singolo... con una vista a tab"). `ChecklistPanel.tsx` è però un componente condiviso, montato anche da `VolunteerRequestDetail.tsx` (vista volontario) con lo stesso pattern a pannello singolo, condizionato su `request.checklistId` (campo singolare). Una volta che il layer di scrittura smette di popolare `checklistId` a favore di `checklistIds: string[]` (come previsto da questa stessa Epic), `request.checklistId` sarà sempre `undefined` per ogni nuova checklist creata: la condizione `{request.checklistId && (...)}` in `VolunteerRequestDetail.tsx` non sarà più mai vera, e il volontario assegnato non vedrà più alcuna checklist, senza errori visibili (regressione silenziosa). Nota minore correlata: il commento JSDoc di `ChecklistPanel.tsx` cita anche un terzo consumer, `ManageableRequestDetail.tsx`, che in realtà non usa né `ChecklistPanel` né `checklistId` — riferimento nel commento non più accurato, non un problema funzionale.
- **Evidenza**:
  - `enable-device/src/pages/volunteer/VolunteerRequestDetail.tsx:237-241` — `{request.checklistId && (<ChecklistPanel requestId={id as string} checklistId={request.checklistId} />)}`
  - `enable-device/src/pages/admin/requests/RequestDetail.tsx:1263-1268` — stesso pattern lato admin, unico consumer esplicitamente in scope nella descrizione dell'Epic EA-129
  - `enable-device/src/components/checklist/ChecklistPanel.tsx:69-70` — commento che cita `VolunteerRequestDetail`, `ManageableRequestDetail` come consumer condivisi
  - `enable-device/src/pages/volunteer/ManageableRequestDetail.tsx` — nessuna occorrenza di `checklistId`/`ChecklistPanel` (verificato via grep)
- **Story/PR/sessione di provenienza**: rilevato durante il grounding della fase `stories` sull'Epic Jira EA-129, sessione 2026-08-03.
- **Stato**: risolto dalla Story Jira EA-133 ("UI: vista a tab multi-checklist in RequestDetail e VolunteerRequestDetail"), che estende lo scope UI a entrambi i consumer (componente condiviso `DeviceRequestChecklists.tsx`) evitando la regressione.

## F-17: Il bottone "Salva" di un item in `ChecklistPanel.tsx` non diventa mai cliccabile: la memoizzazione `BodyCell` di PrimeReact DataTable ignora lo stato locale `drafts`

- **Descrizione**: `ChecklistPanel.tsx` traccia le modifiche non salvate di un item (titolo, assegnatario, quantità, note, stato) in uno stato locale `drafts` separato dall'array `checklist.items` passato a `DataTable` come `value`. Il bottone "Salva" di ogni riga è `disabled={!isDirty(item.id)}`, dove `isDirty` legge `drafts[item.id]`. PrimeReact (`primereact@10.9.7`) memoizza però il componente `BodyCell` con un comparator selettivo che confronta solo un set fisso di chiavi (`rowData`, `field`, `allowCellSelection`, `isCellSelected`, `editMode`, `index`, `tabIndex`, `editing`, `expanded`, `editingMeta`, `frozenCol`, `alignFrozenCol` — `node_modules/primereact/datatable/datatable.cjs.js`, costante `defaultKeysToCompare` usata da `BodyCell`), che NON include il contenuto restituito dalla render prop `body` delle colonne. Poiché `rowData` (l'oggetto item) non cambia mai quando si modifica un campo (la modifica vive solo in `drafts`, esterno all'item), ogni cella della riga — inclusa quella con l'input appena modificato e quella con il bottone "Salva" — viene considerata "invariata" dal memo e non viene mai ri-renderizzata: né l'input mostra il valore digitato (il valore torna vuoto/originale a ogni tasto, per via del ripristino del valore dei controlled input di React basato sull'ultima prop realmente committata), né il bottone "Salva" riceve mai `disabled={false}`. React inoltre traccia `disabled` a livello di fiber per gli elementi form-interattivi e sopprime la dispatch dell'evento `click` indipendentemente da modifiche dirette al DOM (verificato: rimuovere manualmente l'attributo `disabled` dal nodo `<button>` e invocare `.click()` non fa comunque scattare l'handler `onClick` di React). Il risultato è che il flusso di modifica di un item via UI (`updateDeviceRequestChecklistItem`) è nella pratica irraggiungibile da interazione utente, in qualunque ambiente (non solo jsdom: il meccanismo di controlled-input e la memoizzazione sono standard React/PrimeReact, non specifici del test).
- **Evidenza**:
  - `enable-device/src/components/checklist/ChecklistPanel.tsx:141-145` — `setDraftField`/`isDirty`, stato `drafts` esterno a `checklist.items`
  - `enable-device/src/components/checklist/ChecklistPanel.tsx:314-390` — colonne `body` che leggono `getDraft(item)` (merge di `item` e `drafts[item.id]`)
  - `enable-device/src/components/checklist/ChecklistPanel.tsx:399-401` — bottone "Salva", `disabled={!isDirty(item.id)}`
  - `enable-device/node_modules/primereact/datatable/datatable.cjs.js` — `defaultKeysToCompare` e `BodyCell = React.memo(..., comparator)` (righe ~1795-1808 nella versione installata)
  - Riprodotto durante la stesura dei test di regressione EA-133 (`enable-device/src/components/checklist/ChecklistPanel.test.tsx`): digitare in un campo della riga non aggiorna mai il DOM, il bottone "Salva" resta `disabled` anche dopo la modifica, e forzare la rimozione dell'attributo `disabled` + `.click()` nativo non invoca comunque l'handler `onClick` di React.
- **Story/PR/sessione di provenienza**: scoperto durante l'implementazione della Story Jira EA-133 (Epic EA-129), sessione 2026-08-04, mentre si scriveva il test di regressione per l'inoltro di `checklistId` esplicito a `updateDeviceRequestChecklistItem` (non correlato al contenuto di questa Story, che tocca solo il payload inviato, non la logica di dirty-tracking o il DataTable).
- **Stato**: risolto, su richiesta esplicita dell'operatore, sessione 2026-08-04. Fix: la `DataTable` riceve ora `value={checklist.items.map(getDraft)}` invece di `value={checklist.items}` — l'array passato alla tabella contiene già gli item fusi con i rispettivi `drafts`, quindi `rowData` cambia realmente a ogni modifica e il comparator `defaultKeysToCompare` di PrimeReact (che confronta `rowData` per riferimento) rileva correttamente il cambiamento. Le colonne non chiamano più `getDraft(item)` al proprio interno (ridondante, l'item ricevuto è già il draft). Verificato con mutation test manuale: ripristinando `value={checklist.items}` il nuovo test di regressione fallisce esattamente come previsto. Nessuna azione residua.

## F-18: Decisione esplicita di non introdurre dual-read sul campo legacy `checklistId`, dopo verifica empirica che non esistono dati reali dietro quel campo

- **Descrizione**: il panel review automatico di EA-133 ha segnalato un CONCERNS di convenzione: `DeviceRequestChecklists.tsx` legge solo `checklistIds[]`, senza fallback sul vecchio campo singolare `checklistId`, in apparente violazione della convenzione dual-read applicata altrove nel progetto (es. migrazione `assignedVolunteer` → `assignedVolunteers`). Su richiesta esplicita dell'operatore, è stata verificata empiricamente la premessa del concern invece di applicare la convenzione per default: interrogato Firestore staging (`enableitalia-staging`), su 137 `deviceRequests` solo 4 avevano ancora `checklistId` legacy popolato (con `checklistIds` vuoto) — tutti riconducibili a documenti di test dell'operatore, non a dati reali di utilizzo. Produzione (`enableitalia`) confermata pulita dall'operatore (nessun controllo diretto eseguito su produzione). Decisione: nessun dual-read voluto per questa transizione — i 4 documenti di test sono stati bonificati (campo `checklistId` rimosso via `FieldValue.delete()`), e il fallback difensivo `legacyChecklistId` introdotto in `deviceRequestChecklistAccess.ts` da EA-131 "per coesistenza" (mai stato necessario a runtime, solo codice preventivo) è stato rimosso come codice morto, insieme ai test che ne verificavano il comportamento (ora invertiti per asserire `not-found`).
- **Evidenza**:
  - Query diretta su staging (`admin.firestore()`, sessione 2026-08-04): 4/137 `deviceRequests` con `checklistId` legacy, tutti con `checklistIds: []`
  - `functions/backend/src/device-requests/deviceRequestChecklistAccess.ts` — rimosso `legacyChecklistId`/fallback, resta solo `checklistIds.includes(checklistId)`
  - `functions/backend/src/device-requests/{getDeviceRequestChecklist,createChecklistShareLink,getDeviceRequestChecklistCompleteness,removeDeviceRequestChecklistItem,updateDeviceRequestChecklistItem,addDeviceRequestChecklistItem}.test.ts` — test "legacy" invertiti da successo a `not-found`
- **Story/PR/sessione di provenienza**: rilevato dal panel review automatico durante `close-story --auto` su EA-133 (Epic EA-129), deciso e risolto dall'operatore nella stessa sessione, 2026-08-04.
- **Stato**: risolto. Decisione esplicita dell'operatore che sovrascrive il suggerimento del panel review, motivata da verifica empirica (non da ipotesi) sull'assenza di dati reali. Nessuna azione residua.

## F-21: `npm run lint` su `functions/backend` falla già in `develop`/`main` con 113 errori pre-esistenti sparsi su piu' moduli, incluso il blocco notifiche di `changeStatus.ts` non toccato da EA-106

- **Descrizione**: eseguendo `npm run lint` nella working directory di `functions/backend` durante l'implementazione di EA-106, il comando fallisce con 113 errori/10 warning distribuiti su numerosi file (`shipmentRequests.ts`, `utils/email.ts`, `volunteer/setUserRole.ts`, `volunteer/updateVolunteerProfile.ts`, `volunteer/volunteerState.ts`, e altri), non riconducibili a questa Story. In particolare `changeStatus.ts` presenta 3 errori di indentazione (`indent`) nel blocco "Recupera requestNumber da publicDeviceRequests" delle notifiche opzionali (righe ~102-104 dopo il refactoring, ~117-119 prima), un blocco che EA-106 non modifica: verificato con `git stash` che gli stessi 3 errori erano gia' presenti identici prima di questa Story. Non e' chiaro se il progetto abbia mai avuto una baseline lint verde o se `npm run lint` non sia eseguito in CI.
- **Evidenza**:
  - `functions/backend/src/device/changeStatus.ts` — 3 errori `indent` invariati pre/post refactoring EA-106 (verificato con `git stash`/`git stash pop`)
  - `npx eslint --ext .js,.ts .` da `functions/backend` — 113 errori totali su file non toccati da EA-106 (`shipments/shipmentRequests.ts`, `utils/email.ts`, `volunteer/setUserRole.ts`, `volunteer/updateVolunteerProfile.ts`, `volunteer/volunteerState.ts`, tra gli altri)
  - I due file nuovi introdotti da EA-106 (`applyStatusChangeTransaction.ts` e i relativi `.test.ts`) risultano puliti se lintati isolatamente
- **Story/PR/sessione di provenienza**: rilevato durante l'implementazione della Story Jira EA-106, sessione 2026-08-04.
- **Stato**: risolto (fix diretto, 2026-08-12, su richiesta esplicita dell'operatore). Dei 120 problemi rilevati al momento del fix (110 errori/10 warning, la baseline era leggermente cresciuta da 113/10), 102 errori erano risolvibili meccanicamente con `eslint --fix` (`indent`/`quotes`, concentrati per il 60% in un solo file, `createDeviceRequestInternal.ts`); i restanti 6 `no-useless-escape` (`volunteerState.ts`, escape superflui dentro un template literal) e l'errore di parsing su `jest.config.js` (non incluso in alcun tsconfig) sono stati corretti a mano. `npm run lint` esce ora con **0 errori** (10 warning residui, tutti `no-explicit-any`/`no-non-null-assertion` — miglioramenti di tipizzazione che richiedono una valutazione caso per caso, esplicitamente fuori scope qui).

## F-20: `functions/backend/src/utils/telegram.ts` importa il pacchetto ESM-only `jose`, che rompe il parsing Jest (`ts-jest`) per qualunque test che importi transitivamente quel modulo

- **Descrizione**: `telegram.ts` fa `import { SignJWT } from "jose"`. Il pacchetto `jose` installato (`node_modules/jose`) è pubblicato come ESM puro; `ts-jest` con la config corrente (`jest.config.js`, nessun `transformIgnorePatterns` che includa `jose`) non lo trasforma, quindi qualunque test che importi (anche transitivamente) `telegram.ts` fallisce con `SyntaxError: Unexpected token 'export'` prima ancora di eseguire un solo test. `device/changeStatus.ts` importa `telegram.ts` per la notifica opzionale via Telegram, quindi il primo test scritto per `changeStatus.ts` (introdotto in questa Story) ha incontrato il problema. Nessun test preesistente nel repo importava `changeStatus.ts` o `telegram.ts`, quindi il problema non era mai stato osservato prima.
- **Evidenza**:
  - `functions/backend/src/utils/telegram.ts:1` — `import { SignJWT } from "jose"`
  - `functions/backend/jest.config.js` — preset `ts-jest`, nessuna configurazione di `transformIgnorePatterns` per pacchetti ESM in `node_modules`
  - Riprodotto in sessione: `npx jest src/device/changeStatus.test.ts` senza mock di `../utils/telegram` fallisce con `SyntaxError: Unexpected token 'export'` risalendo a `node_modules/jose/dist/webapi/index.js:1`
- **Story/PR/sessione di provenienza**: rilevato durante l'implementazione della Story Jira EA-103 (isolamento RBAC di `isAllowedVolunteerTransition`), sessione 2026-08-04, scrivendo `device/changeStatus.test.ts`.
- **Stato**: risolto (fix diretto, 2026-08-12, su richiesta esplicita dell'operatore). Verificata prima l'opzione `transformIgnorePatterns: ["node_modules/(?!jose)"]` in isolamento: non sufficiente da sola, perché `transform` mappa solo i file `.ts` a `ts-jest` — l'output compilato di `jose` è `.js`, per cui resterebbe comunque non trasformato senza aggiungere anche un transform per `.js` (es. `babel-jest`, non presente come dipendenza diretta). Scelta invece l'opzione alternativa già indicata dal finding: sostituito `jose` con `jsonwebtoken` (CommonJS, già presente come dipendenza transitiva, ora dichiarata esplicitamente) in `telegram.ts` — stesso algoritmo HS256, stessa scadenza 5 minuti, nessun cambio di comportamento verso l'endpoint Telegram. Aggiunto `telegram.test.ts`, deliberatamente senza alcun mock di `jsonwebtoken`, come prova di regressione che l'importazione diretta ora funziona in Jest. Il workaround locale in `changeStatus.test.ts` (mock di `changeStatusNotifications`) resta invariato — esiste anche per isolare il test da side-effect reali, non solo per il problema `jose`.

## F-19: dopo EA-102, il backend scrive `publicStatus: "da validare"` su `publicDeviceRequests`, valore assente dal tipo TS `publicStatus` e da alcuni elenchi di gruppo hardcoded lato frontend

- **Descrizione**: EA-102 corregge `mapToPublicStatus.ts` (backend) perché `"inviata"` restituisca `"da validare"` invece di `"da gestire"`, come esplicitamente richiesto dallo scope della Story (solo quel file). Il frontend (`enable-device`) conosce già il concetto "da validare" come gruppo derivato localmente (`PUBLIC_STATUS_GROUPS["da validare"] = ["inviata"]` in `enable-device/src/shared/types/deviceData.ts`, usato per la classificazione in `AdminLayout.tsx`) e ha già `PUBLIC_STATUS_SEVERITY["da validare"]` definito, quindi il rendering dello stato (`Tag`) non si rompe. Tuttavia due punti restano disallineati rispetto al valore che il backend ora scrive realmente nel campo persistito `publicDeviceRequests/{id}.publicStatus`: (1) il tipo `publicStatus` in `enable-device/src/shared/types/deviceData.ts:6-10` è un'unione di sole 4 stringhe, che non include `"da validare"` — nessun errore a runtime (i dati Firestore non sono type-checked), ma un potenziale disallineamento silenzioso per qualunque codice futuro tipizzato su `publicDeviceRequestData.publicStatus`; (2) `VolunteerDashboard.tsx:200` definisce `INACTIVE_PUBLIC_STATUSES = ["annullate / non completabili", "da gestire"]` per escludere richieste dal conteggio "attive" del grafico a torta (righe ~199-210, alimentato dal campo `publicStatus` letto direttamente da `publicDeviceRequests`, non ricalcolato lato frontend): prima di questa Story le richieste appena inviate avevano `publicStatus: "da gestire"` ed erano quindi escluse dal conteggio; dopo questa Story hanno `publicStatus: "da validare"`, valore non presente in `INACTIVE_PUBLIC_STATUSES`, quindi ora vengono conteggiate come "attive" nel grafico del volontario — un cambiamento di comportamento visivo minore, non richiesto né vietato esplicitamente dagli scenari Gherkin della Story (che riguardano solo il valore restituito da `mapToPublicStatus`, non il consumo lato frontend). Non risulta invece alcun impatto sulla query di "richieste gestibili" (`daGestireStatuses` in `VolunteerDashboard.tsx:90` e `ManageableRequestsPage.tsx:117`), perché quella query filtra sul campo interno `status` usando il gruppo `PUBLIC_STATUS_GROUPS["da gestire"]` definito localmente lato frontend, che già escludeva `"inviata"` prima ancora di questo fix backend.
- **Evidenza**:
  - `functions/backend/src/utils/mapToPublicStatus.ts` (questa Story) — `"inviata"` ora restituisce `"da validare"`
  - `enable-device/src/shared/types/deviceData.ts:6-10` — `export type publicStatus = "da gestire" | "fabbricazione in corso" | "completati" | "annullate / non completabili"` (manca `"da validare"`)
  - `enable-device/src/pages/volunteer/VolunteerDashboard.tsx:200-210` — `INACTIVE_PUBLIC_STATUSES` non include `"da validare"`; conteggio "attive" alimentato da `r.publicStatus` letto da `publicDeviceRequests`
  - `enable-device/src/pages/volunteer/VolunteerDashboard.tsx:90`, `enable-device/src/pages/volunteer/ManageableRequestsPage.tsx:117` — query "gestibili" già isolate dal bug, filtrano su `status` interno con gruppo locale che esclude `"inviata"`
- **Story/PR/sessione di provenienza**: rilevato durante l'implementazione della Story Jira EA-102 (Epic EA-2), sessione 2026-08-04. Fuori perimetro dichiarato dalla Story (solo `functions/backend/src/utils/mapToPublicStatus.ts`), non corretto qui.
- **Stato**: risolto strutturalmente dalla Story Jira EA-149 (2026-08-07): il campo `publicStatus` viene eliminato per intero dal flusso di scrittura backend (`applyStatusChangeTransaction.ts`, `createDeviceRequest.ts`, `createDeviceRequestInternal.ts` non lo scrivono più), quindi il disallineamento di tipo qui descritto non ha più un campo persistito da cui originare. Resta però aperta, non toccata da EA-149, la riscrittura dei consumer frontend che leggono ancora `publicStatus`/`publicStatus2` da dati storici o dal proprio calcolo locale (`VolunteerDashboard.tsx` incluso `INACTIVE_PUBLIC_STATUSES`) — esplicitamente fuori scope di EA-149 ("Riscrittura dei consumer frontend... vedi Story dedicata"), riferimento [[F-22]].

## F-22: `mapToPublicStatus` (backend) e `mapInternalStatusToPublic` (frontend) sono disallineate sul gruppo pubblico dello stato `inviata` — stessa causa di [[F-19]], framing indipendente

- **Descrizione**: le due implementazioni del mapping stato-interno → stato-pubblico, oggi duplicate indipendentemente in backend e frontend, non sono solo duplicate ma già divergenti nel comportamento. Il frontend (`enable-device/src/helpers/requestStatus.ts`, `PUBLIC_STATUS_GROUPS`/`mapInternalStatusToPublic`) tratta esplicitamente `inviata` come gruppo pubblico separato `"da validare"` (con un commento nel codice che lo motiva: "'da validare' non è un publicStatus Firestore: è derivato da status==='inviata'"). Il backend (`functions/backend/src/utils/mapToPublicStatus.ts`, invariato da questa Story) include invece `inviata` nel gruppo `"da gestire"` insieme agli altri 4 stati (`famiglia contattata`, `definizione richiesta`, `valutazione fattibilità`, `attesa volontario`). Il campo `publicStatus` scritto in Firestore da `changeStatus.ts`/`createDeviceRequest.ts` per una richiesta appena `inviata` è quindi sempre `"da gestire"`, mai `"da validare"` — la vista frontend che raggruppa per pubblico usa una logica di raggruppamento diversa da quella effettivamente persistita lato backend per lo stesso stato. Trovata indipendentemente durante EA-107 (che non aveva ancora visibilità sulla PR di EA-102, già mergiata su `develop` nel frattempo, che descrive la stessa divergenza da un'angolatura complementare — vedi [[F-19]]): qui si riparte dal commento di refinement originale dell'Epic EA-2, là dall'effetto concreto del fix di `mapToPublicStatus.ts` sul frontend.
- **Evidenza**:
  - `functions/backend/src/utils/mapToPublicStatus.ts` (ora `functions/backend/src/utils/productionLifecycle.ts` + `mapToPublicStatus.ts` dopo EA-107) — `daGestire` include `"inviata"`
  - `enable-device/src/helpers/requestStatus.ts:52-60` — `PUBLIC_STATUS_GROUPS["da validare"] = ["inviata"]`, gruppo separato da `"da gestire"`
  - `enable-device/src/helpers/requestStatus.ts:89-96` — `mapInternalStatusToPublic` itera `PUBLIC_STATUS_GROUPS` e trova `"inviata"` nel gruppo `"da validare"` prima di `"da gestire"`
- **Story/PR/sessione di provenienza**: discrepanza già osservata nel commento di refinement dell'Epic EA-2 (data di refinement precedente a questa Story); registrata formalmente in questo file durante l'implementazione della Story Jira EA-107 (centralizzazione backend degli 8 stati di fabbricazione), sessione 2026-08-04. Esplicitamente fuori scope da EA-107, che tocca solo il perimetro backend (`mapToPublicStatus.ts`/`changeStatus.ts`) e lascia `requestStatus.ts` invariato per non introdurre una modifica di comportamento osservabile senza pianificazione dedicata.
- **Stato**: risolto strutturalmente dalla Story Jira EA-149 (2026-08-07), come previsto dalla sua stessa descrizione: eliminando per intero `publicStatus`/`mapToPublicStatus.ts` dal backend, le due implementazioni divergenti non esistono più contemporaneamente — non serve più riconciliarle, perché quella backend è stata rimossa. Resta pero' un'eco non risolta lato frontend: `enable-device/src/helpers/requestStatus.ts` (`mapInternalStatusToPublic`, non toccato da EA-149) continua a derivare un raggruppamento pubblico display-only con la stessa logica precedente (gruppo `"da validare"` separato per `inviata`), ora l'unica fonte di quel raggruppamento essendo sparito il campo persistito — coerente in sé, ma da tenere presente se in futuro si introdurrà un helper di raggruppamento condiviso lato backend, riferimento [[F-19]].

## F-23: refine dell'Epic EA-134 (2026-08-05) rimuove la denormalizzazione di `category`/`origin` su `checklistItems`, ma non chiarisce come `listMyChecklistItems` applicherà ancora il filtro opzionale per `scope`

- **Descrizione**: la versione originale dell'Epic EA-134 descriveva `listMyChecklistItems(uid, scope?)` con una query Firestore a due filtri diretti sulla collection `checklistItems`: `where('assignee','==', uid)` più, opzionalmente, `where('category','==', scope)` — reso possibile dalla denormalizzazione di `category` su ogni documento `checklistItems` prevista dalla mitigazione originale dello studio deciso `ss-user-todo-list`/opt-e. Il commento di refinement del 2026-08-05 rimuove esplicitamente quella denormalizzazione (punto 3): `category`/`origin` non vengono più scritti sul singolo `checklistItems`, e `listMyChecklistItems` li risolve invece con una batch-read (`db.getAll`) delle checklist padre distinte referenziate dagli item pending restituiti — un meccanismo pensato per popolare `category`/`origin` nella risposta, non per filtrare la query. Il commento non affronta esplicitamente la conseguenza diretta: senza il campo `category` su `checklistItems`, il filtro opzionale `where('category','==', scope)` descritto dall'Epic non è più eseguibile così com'è a livello Firestore. Non è chiaro se `scope` debba essere: (a) abbandonato come parametro di query diretta e applicato come post-filtro dopo la batch-read dei checklist padre, (b) implementato altrimenti, o (c) rimosso del tutto da questa versione della capability. La stessa ambiguità si riflette sullo spike non-blocking `spike-composite-index-assignee-scope` (già individuato in `ss-user-todo-list`, fuori scope dell'Epic): la sua premessa — "query a due filtri" che potrebbe richiedere un indice composito — presuppone il filtro Firestore diretto che questo stesso refine rimuove.
- **Evidenza**: descrizione Epic Jira EA-134 (campo `Dominio/i target e capability collegate`, bullet `process-organizer-core/cap-checklist-item-management`) prima e dopo il refine 2026-08-05; commento umano su EA-134, punto 3, 2026-08-05T11:35:53.783+0200.
- **Story/PR/sessione di provenienza**: rilevato durante la sessione di refine (fase `epic`) dell'Epic Jira EA-134, 2026-08-05, applicando il commento umano al testo esistente dell'Epic.
- **Stato**: risolto. Commento umano successivo su EA-134, 2026-08-05T12:24:03.777+0200 ("risoluzione EA-136/F-23"): `scope` corrisponde a `category` (non a `origin`); `category` torna a essere denormalizzata su ogni `checklistItems` al momento della scrittura (`create`/`add`/`update`/`clone`/`createFromTemplate`), come nella mitigazione originale di opt-e — supera parzialmente il punto 3 del refine precedente, che va ora inteso come: solo `origin` resta non denormalizzato (risolto via batch-read per la sola navigabilità), `category` sì. Il filtro `where('assignee','==', uid).where('category','==', scope)` in `listMyChecklistItems` è quindi eseguibile direttamente su `checklistItems` come originariamente descritto, senza bisogno del batch-read per questo caso; anche la premessa "query a due filtri" dello spike `spike-composite-index-assignee-scope` torna valida. Epic EA-134 aggiornata di conseguenza (sezione `Dominio/i target e capability collegate`) durante la sessione di refine (fase `epic`) del 2026-08-05 che ha applicato questo commento; Task Jira EA-136 (creato per tracciare questa decisione) chiuso in `Done` nella stessa sessione.
## F-24: dopo EA-137, tre funzioni di lettura non toccate da questa Story leggono ancora `checklists/{id}.items` aspettandosi oggetti item completi, che ora e' un array di soli `itemId` — regressione funzionale silenziosa per ogni checklist creata/modificata dopo il deploy

- **Descrizione**: la Story EA-137 (questa sessione) riscrive le 4 funzioni di scrittura del core Organizer (`createChecklist`, `addChecklistItem`, `updateChecklistItem`, `removeChecklistItem`) perché scrivano gli item come documenti distinti nella nuova collection `checklistItems/{itemId}`, riducendo `checklists/{id}.items` a un array di soli `itemId` (decisione ss-user-todo-list/opt-e, Epic EA-134). La descrizione della Story elenca esplicitamente `getChecklist` e `getChecklistCompleteness` come "Fuori scope... Story separata", riconoscendo che sono lettori del vecchio formato. Non cita pero' un terzo lettore con la stessa dipendenza diretta: `device-requests/getChecklistShareStatus.ts` (Story EA-113), che legge `checklistSnap.data().items` assumendolo un array di `ChecklistItemLike` e lo passa a `isChecklistItemComplete`/`isChecklistComplete` (`checklistCompleteness.ts`) per calcolare `percentComplete`. Dopo il deploy di questa Story, per qualunque checklist creata o modificata con le nuove funzioni, `items` conterra' stringhe (`itemId`) invece di oggetti: passate a `isChecklistItemComplete`, ogni stringa viene letta come oggetto senza `type`/`assignee`/`status` (tutti `undefined`), ricade nel calcolo `generic` e risulta sempre non completa (`hasAssignee` falso). Il risultato e' che **tutte e tre** le funzioni di lettura (`getChecklist`, `getChecklistCompleteness`, `getChecklistShareStatus`) restituiranno dati silenziosamente errati (item mancanti/vuoti, `percentComplete` sempre 0% per checklist non vuote, eccetto il caso vacuo "0 item = 100%") per ogni checklist toccata dalle funzioni di scrittura riscritte in questa Story — senza errori visibili, un comportamento analogo al deploy sequenziale gia' segnalato in [[F-14]]. Le checklist create prima di questo deploy (con `items` ancora array di oggetti embedded) continuano invece a funzionare correttamente con le funzioni di lettura invariate, fino a quando non vengono modificate da una delle 4 funzioni riscritte.
- **Evidenza**:
  - `functions/backend/src/organizer/getChecklist.ts:54` — `items: Array.isArray(data.items) ? data.items : []` restituito cosi' com'e' al consumer, assumendo oggetti item completi
  - `functions/backend/src/organizer/getChecklistCompleteness.ts:43-45` — `const items: ChecklistItemLike[] = Array.isArray(data.items) ? data.items : []; const complete = isChecklistComplete(items);`
  - `functions/backend/src/device-requests/getChecklistShareStatus.ts:52-55` — stesso pattern, non nominata nella sezione "Fuori scope" della Story EA-137 ne' dell'Epic EA-134
  - `functions/backend/src/organizer/checklistCompleteness.ts:50-52` — `hasAssignee` richiede `typeof item.assignee === "string"`, sempre falso per un elemento stringa (`itemId`)
- **Story/PR/sessione di provenienza**: rilevato durante l'implementazione della Story Jira EA-137 (Epic EA-134), sessione 2026-08-05, mentre si verificava l'impatto della riscrittura delle funzioni di scrittura sui consumer esistenti del campo `checklists/{id}.items`.
- **Stato**: risolto parzialmente. La Story Jira EA-138 (2026-08-05) ha implementato la ricostruzione a due fasi (risoluzione batch degli `itemId` in `checklistItems` via `db.getAll`, modulo condiviso `resolveChecklistItems.ts`) per `getChecklist` e `getChecklistCompleteness`, esattamente i due lettori nominati dal suo scope dichiarato — vedi `functions/backend/src/organizer/getChecklist.ts`, `getChecklistCompleteness.ts`, `resolveChecklistItems.ts`. Il terzo lettore identificato da questo finding, `functions/backend/src/device-requests/getChecklistShareStatus.ts:52-55`, resta pero' non corretto: la descrizione della Story EA-138 elenca esplicitamente solo `getChecklist`/`getChecklistCompleteness`, non lo cita, e la sessione di implementazione non lo ha quindi toccato (fuori dal perimetro dichiarato, per design del flusso di implementazione a una Story = un branch). Il rischio di deploy sequenziale segnalato resta quindi aperto per questa sola funzione: qualunque checklist creata/modificata dopo il deploy di EA-137 fa calcolare a `getChecklistShareStatus` un `percentComplete` errato (tipicamente 0% per checklist non vuote). Proposta: aprire un Task Jira per applicare lo stesso pattern di risoluzione (`resolveChecklistItems`, gia' pronto e riusabile) a `getChecklistShareStatus.ts`, riferimento F-24.

## F-26: dopo EA-141, `addDeviceRequestChecklistItem.ts` resta l'unico endpoint del layer device-requests che accetta ancora `assignee` come stringa libera, mai risolta a un uid Firebase reale

- **Descrizione**: la Story EA-141 promuove `assignee` a identità reale solo in `updateDeviceRequestChecklistItem.ts` (unico file elencato nella sezione "Cosa cambia" della Story per la validazione lato backend): da questa Story in poi, un `assignee` passato a `updateDeviceRequestChecklistItem` deve risolvere a un uid tra gli `assignedVolunteers` della `deviceRequest` collegata o a un admin, altrimenti la richiesta è rifiutata con `invalid-argument`. `addDeviceRequestChecklistItem.ts` (creazione di un nuovo item, con `assignee` opzionale nel payload) non applica alcuna validazione equivalente e delega direttamente ad `addChecklistItem` del core Organizer senza passare da `isResolvableChecklistAssignee`: resta quindi possibile creare un item con `assignee` arbitrario (stringa libera, non uid reale) tramite quell'endpoint. Il frontend (`ChecklistPanel.tsx`, in scope per questa Story) è stato comunque aggiornato a presentare la stessa selezione tra utenti reali risolti anche nel dialog "Aggiungi item" (che chiama `addDeviceRequestChecklistItem`), per coerenza UI e perché lo scenario Gherkin 3 non distingue tra creazione e modifica di un item — ma questo è un vincolo solo lato client: una chiamata diretta a `addDeviceRequestChecklistItem` (es. da uno script, o bypassando la UI) può ancora impostare un `assignee` libero non risolvibile.
- **Evidenza**:
  - `functions/backend/src/device-requests/addDeviceRequestChecklistItem.ts` — nessuna chiamata a `isResolvableChecklistAssignee`/`resolveDeviceRequestChecklistAccess` per validare `assignee`, a differenza di `updateDeviceRequestChecklistItem.ts` (questa Story)
  - Descrizione Story Jira EA-141, sezione "Cosa cambia": elenca esplicitamente solo `updateDeviceRequestChecklistItem.ts`, `deviceRequestChecklistAccess.ts`, `ChecklistPanel.tsx`
- **Story/PR/sessione di provenienza**: rilevato durante l'implementazione della Story Jira EA-141, sessione 2026-08-06, mentre si delimitava lo scope della validazione lato backend agli scenari Gherkin (che menzionano solo `updateDeviceRequestChecklistItem`).
- **Stato**: risolto (fix diretto, 2026-08-12, su richiesta esplicita dell'operatore). `addDeviceRequestChecklistItem.ts` valida ora `assignee` con lo stesso controllo di `updateDeviceRequestChecklistItem.ts` (`isResolvableChecklistAssignee`): deve risolvere a un `assignedVolunteers` della richiesta o a un admin, altrimenti `invalid-argument`. `assignee` omesso o `null` salta la validazione (item non assegnato).

## F-27: la selezione "admin" nel dropdown Assegnatario di `ChecklistPanel.tsx` è limitata all'utente correntemente autenticato, non all'intero roster admin

- **Descrizione**: lo scenario Gherkin 3 di EA-141 richiede che il controllo UI mostri "una selezione tra utenti reali risolti (es. volontari assegnati/admin)", senza specificare se l'insieme completo degli admin debba essere selezionabile o solo l'admin che sta operando. Le regole Firestore attuali (`firestore.rules`, `match /users/{userId}`) permettono a un volontario di leggere solo il proprio documento `users/{uid}` (non quello di altri utenti), quindi non esiste, senza introdurre una nuova Cloud Function o un allentamento delle regole di sicurezza (entrambi fuori dal perimetro di file dichiarato dalla Story: solo `deviceRequestChecklistAccess.ts`, `updateDeviceRequestChecklistItem.ts`, `ChecklistPanel.tsx`), un modo per il client di enumerare "tutti gli admin". La scelta implementativa adottata è quindi: le opzioni del dropdown sono gli `assignedVolunteers` della `deviceRequest` (leggibili da qualunque volontario/admin autenticato) più l'utente corrente, se il suo stesso ruolo è `admin` — non l'intero roster admin dell'organizzazione. Backend (`isResolvableChecklistAssignee`) resta comunque più permissivo della UI: accetta come `assignee` valido qualunque uid con ruolo `admin`, non solo l'utente corrente, quindi non c'è un problema di sicurezza, solo un limite di usabilità (un admin non può assegnare un item a un *altro* admin dalla UI, deve farlo un domani con un endpoint dedicato o passando dati diversi).
- **Evidenza**:
  - `enable-device/src/components/checklist/ChecklistPanel.tsx` — `loadAssignableUsers`: `candidateUids` include `assignedVolunteers` più `selfUid` solo se `role === "admin"`
  - `firestore.rules:90-96` — lettura di `users/{userId}` (documento radice, contiene `role`) permessa solo ad admin o al proprietario stesso (`request.auth.uid == userId`)
- **Story/PR/sessione di provenienza**: rilevato durante l'implementazione della Story Jira EA-141, sessione 2026-08-06, progettando la risoluzione degli utenti assegnabili in `ChecklistPanel.tsx` nel perimetro di file dichiarato dalla Story.
- **Stato**: risolto (fix diretto, 2026-08-12, su richiesta esplicita dell'operatore). Nuova Cloud Function `listAssignableChecklistUsers.ts` risolve lato server `assignedVolunteers` della richiesta + tutti gli admin (query `where("role","==","admin")` su `users`, ammessa lato Admin SDK); `ChecklistPanel.tsx` la usa al posto della risoluzione client-side precedente.

## F-28: il commento di `handleValidate` in `RequestDetail.tsx` descrive un target di transizione (`"famiglia contattata"`) diverso da quello scritto realmente dal codice (`"validata"`)

- **Descrizione**: il blocco di commento che precede `handleValidate` (righe 521-526) descrive il passo 3 del flusso di validazione come "Cambia lo status interno a 'famiglia contattata' via cloud function". Il corpo della funzione, poche righe sotto, chiama invece `changeStatus` con `newStatus: "validata"` (riga 561) — coerente con il resto della UI, che al termine della validazione mostra il messaggio "La richiesta è ora visibile ai volontari" e con l'annotazione altrove nello stesso file (riga 1554) che descrive esplicitamente lo stato `"validata"` con `publicStatus` `"da gestire"`. Il comportamento a runtime è quindi corretto e coerente con lo stato `validata` documentato nella richiesta `ss-device-request-macro-status.md`; solo il commento è disallineato, verosimilmente non aggiornato dopo un refactoring del flusso di validazione. Rilevanza per questo studio: la sessione di refine ha verificato esplicitamente che `"validata"` (non `"famiglia contattata"`) è il target reale di `handleValidate`, per confermare che `"validata"` resta uno stato distinto e non rientra nel collasso dei 4 "prerequisiti di fabbricazione" (`famiglia contattata`, `definizione richiesta`, `valutazione fattibilità`, `attesa volontario`) proposto in opt-b — la scoperta del commento disallineato è stata un effetto collaterale di quella verifica, non il suo obiettivo.
- **Evidenza**:
  - `enable-device/src/pages/admin/requests/RequestDetail.tsx:521-526` — commento: "3. Cambia lo status interno a 'famiglia contattata' via cloud function"
  - `enable-device/src/pages/admin/requests/RequestDetail.tsx:558-563` — codice: `fn({ requestId: id, newStatus: "validata", note: "Richiesta validata dall'amministratore." })`
  - `enable-device/src/pages/admin/requests/RequestDetail.tsx:1554` — riferimento coerente altrove nello stesso file a `status` «validata» e `publicStatus` «da gestire»
- **Story/PR/sessione di provenienza**: rilevato durante la sessione di refine (2026-08-06) dello studio `ss-device-request-macro-status`, verificando nel codice quali stati siano coinvolti dal collasso esteso proposto dal supervisor.
- **Stato**: risolto. Corretto durante l'implementazione della Story Jira EA-149 (2026-08-07), che ha toccato lo stesso blocco di commento (righe 521-526) per rimuovere il riferimento a `publicStatus`: il commento ora descrive correttamente il passo 3 come "Cambia lo status interno a 'validata' via cloud function".

## F-29: dopo EA-148, il dominio backend di `deviceRequests.status` è a 11 valori ma i consumer frontend (dropdown/costanti) restano ancora sui 19 valori pre-riduzione — nessun modo per admin/volontario di selezionare `da gestire`/`in produzione` dalla UI esistente

- **Descrizione**: la Story EA-148 (decisione opt-b di `ss-device-request-macro-status`) riduce il dominio effettivo di `status` gestito dal backend (`utils/productionLifecycle.ts`: 8→4 valori; `utils/volunteerTransitions.ts`: 5→2 coppie), per esplicita indicazione della descrizione della Story stessa, che elenca solo file `functions/backend/src/*` come perimetro e rimanda a Story dedicate sia la rimozione di `publicStatus`/`mapToPublicStatus.ts` sia (implicitamente, per continuità con quello stesso lavoro nello studio di soluzione) la riscrittura dei consumer frontend. `enable-device/src/helpers/requestStatus.ts` (`REQUEST_STATUSES`, usato dai dropdown di `RequestDetail.tsx`, `VolunteerRequestDetail.tsx`, `AdminRequestTable.tsx`) e `enable-device/src/shared/types/deviceData.ts` (`deviceStatus`) non sono stati toccati da questa Story e continuano a esporre i 19 valori originari, inclusi i 10 letterali ora fuori dominio (es. `famiglia contattata`, `followup famiglia ko`, `scelta device e dimensionamento`) — e senza alcuna voce per i 2 nuovi valori collassati `da gestire`/`in produzione`. Poiché `changeStatus.ts` non ha mai validato (né prima né dopo questa Story) il valore di `newStatus` contro un elenco consentito — il backend accetta qualunque stringa per un admin — non c'è alcun errore a runtime, ma fino a quando i consumer frontend non verranno aggiornati: (1) nessun admin/volontario può raggiungere `da gestire`/`in produzione` tramite l'unica UI esistente per cambiare stato; (2) selezionando uno dei 10 valori letterali rimossi dal dropdown, il backend lo accetta comunque e lo scrive, ma quel valore non viene più riconosciuto da `isProductionLifecycleStatus` (per i 5 ex-stati di produzione granulare), quindi `mapToPublicStatus` lo classifica erroneamente in `da gestire` invece che `fabbricazione in corso`.
- **Evidenza**:
  - `enable-device/src/helpers/requestStatus.ts:1-21` — `REQUEST_STATUSES` con i 19 valori originari, non aggiornato
  - `enable-device/src/shared/types/deviceData.ts:12-28` — `deviceStatus` con gli stessi 19 valori
  - `functions/backend/src/utils/productionLifecycle.ts` — dopo EA-148, `PRODUCTION_LIFECYCLE_STATUSES` riconosce solo `["in produzione", "pronta per spedizione", "spedita", "followup famiglia"]`
  - `functions/backend/src/device/changeStatus.ts` — nessuna validazione di `newStatus` contro un dominio consentito, invariato da prima di questa Story
- **Story/PR/sessione di provenienza**: rilevato durante l'implementazione della Story Jira EA-148, sessione 2026-08-07. Non corretto in questa Story: il perimetro dichiarato nella descrizione di EA-148 elenca solo file backend e rimanda esplicitamente a Story dedicate il lavoro di rimozione `publicStatus`/riscrittura consumer, di cui la potatura di `REQUEST_STATUSES`/`deviceStatus` è parte integrante secondo lo studio di soluzione deciso (`docs/solution-studies/ss-device-request-macro-status.json`, sketch di opt-b).
- **Stato**: risolto. Prima estensione additiva durante EA-152 (sessione 2026-08-08): `REQUEST_STATUSES`/`REQUEST_STATUS_SEVERITY` allargati coi 2 valori `da gestire`/`in produzione`, senza rimuovere i 10 ormai fuori dominio. Rimozione completata su richiesta esplicita dell'operatore (sessione 2026-08-09), dopo conferma che la migrazione one-shot era già stata eseguita sull'ambiente rilevante (nessun dato residuo nei 10 valori vecchi): `REQUEST_STATUSES`/`REQUEST_STATUS_SEVERITY`/`REQUEST_STATUS_DESCRIPTIONS`/`CLOSED_STATUSES` in `requestStatus.ts`, e `deviceStatus` in `shared/types/deviceData.ts`, ridotti agli 11 valori del dominio corrente — verificato con grep che nessun consumer (`RequestTable.tsx`, `AdminRequestTable.tsx`, `VolunteerRequestDetail.tsx`, `VolunteerDashboard.tsx`, `RequestDetail.tsx`) dipendesse da indici/conteggi fissi, solo enumerazione diretta dell'array. `REMOVED_STATUS_TO_GENERIC` resta invariata (lo strumento di migrazione deve continuare a riconoscere i 10 valori storici su ambienti dove non sia ancora stata eseguita). Resta **non risolto**, separato e non affrontato qui: `changeStatus.ts` non valida `newStatus` contro un elenco consentito — un valore tra i 10 rimossi, se inviato per una via diversa dal dropdown standard (es. chiamata diretta alla Cloud Function), verrebbe comunque accettato lato backend.

## F-30: lo studio `ss-device-request-macro-status`/opt-b e la Story Jira EA-149 non elencano `createDeviceRequest.ts`/`createDeviceRequestInternal.ts` tra i punti di scrittura di `publicStatus`, pur richiedendo l'eliminazione completa di `mapToPublicStatus.ts`

- **Descrizione**: sia il refine deciso di `ss-device-request-macro-status` (opt-b) sia la descrizione della Story Jira EA-149 elencano esplicitamente due soli punti di scrittura di `publicStatus` da eliminare: `applyStatusChangeTransaction.ts` (righe 31/46 prima di questa Story) e `RequestDetail.tsx`/`handleValidate` (riga 553 prima di questa Story) — quest'ultimo esplicitamente definito "un secondo punto di scrittura indipendente, verificato esplicitamente nello studio di soluzione". Verificando nel codice durante l'implementazione, `mapToPublicStatus` risultava importato anche da altri due file non menzionati né dallo studio né dalla Story: `functions/backend/src/device/createDeviceRequest.ts` (righe 154/190, form pubblica di creazione richiesta) e `functions/backend/src/device/createDeviceRequestInternal.ts` (righe 47/87, creazione interna/import), entrambi scriventi `publicStatus: mapToPublicStatus("inviata")` sia su `deviceRequests` sia su `publicDeviceRequests` al momento della creazione. L'istruzione esplicita della Story ("il file `mapToPublicStatus.ts` viene eliminato per intero") non è eseguibile alla lettera lasciando questi due import in piedi: cancellare il file senza toccarli avrebbe rotto la build. La Story stessa, nella sua nota di chiusura, dichiara di risolvere strutturalmente [[F-19]] — che descrive esplicitamente `createDeviceRequest.ts` come scrittore di `publicStatus` — offrendo un indizio che l'eliminazione dovesse estendersi anche lì, ma senza nominarlo tra i "domini coinvolti" o i file da toccare.
- **Evidenza**:
  - Studio deciso `ss-device-request-macro-status.json`, sezione opt-b (`mitigations[0]`, `rationale`): elenca solo `applyStatusChangeTransaction.ts` e `RequestDetail.tsx`/`handleValidate` riga 553 come punti di scrittura di `publicStatus` da eliminare
  - Descrizione Story Jira EA-149: stesso perimetro dichiarato, nessuna menzione di `createDeviceRequest.ts`/`createDeviceRequestInternal.ts`
  - `functions/backend/src/device/createDeviceRequest.ts:154,190` (prima di questa Story) — `publicStatus: mapToPublicStatus("inviata")` su `deviceRequests` e `publicDeviceRequests`
  - `functions/backend/src/device/createDeviceRequestInternal.ts:47,87` (prima di questa Story) — stesso pattern
  - `docs/FINDINGS.md` F-19 (EA-102): gia' cita `createDeviceRequest.ts` come scrittore di `publicStatus: "da validare"`, senza pero' essere collegato esplicitamente al perimetro di file di EA-149
- **Story/PR/sessione di provenienza**: rilevato durante l'implementazione della Story Jira EA-149, sessione 2026-08-07, nel momento di eliminare `functions/backend/src/utils/mapToPublicStatus.ts` e verificarne tutti gli importatori residui.
- **Stato**: risolto. Interpretando l'istruzione esplicita "il file viene eliminato per intero" come vincolante e coerente con il titolo della Story ("dal flusso di scrittura backend", non solo dal flusso di `changeStatus`), la sessione ha esteso lo stesso pattern di rimozione (niente piu' scrittura di `publicStatus`, import rimosso) anche a `createDeviceRequest.ts` e `createDeviceRequestInternal.ts`, permettendo la cancellazione completa del file senza rompere la build. Nessuno scenario Gherkin della Story copre esplicitamente il flusso di creazione; se il perimetro reale fosse voluto piu' stretto (solo i due punti nominati, con `mapToPublicStatus.ts` da mantenere per i soli scrittori di creazione), va segnalato al prossimo refine — riferimento F-30.

## F-31: quattro consumer di `publicStatus`/`PUBLIC_STATUS_GROUPS` non elencati nel perimetro dei "7 consumer verificati" di EA-150 restano su dati ora stale/mai piu' scritti (EA-149) o su un dominio di status pre-riduzione (EA-148)

- **Descrizione**: lo studio di soluzione deciso `ss-device-request-macro-status`/opt-b e la Story Jira EA-150 elencano esplicitamente 7 consumer frontend da riscrivere per derivare da `status` invece che da `publicStatus`/`publicStatus2`: `AdminDashboard.tsx`, `RequestTable.tsx`, `AdminRequestTable.tsx`, `VolunteerDashboard.tsx`, `VolunteerLayout.tsx`, `RequestDetail.tsx`, `AdminLayout.tsx`. Verificando l'intero codebase durante l'implementazione, altri 4 file leggono/scrivono ancora `publicStatus` o il vecchio `PUBLIC_STATUS_GROUPS` (dominio a 19 valori) senza essere stati inclusi in nessuno dei due perimetri (né EA-148 né EA-149 né EA-150), quindi restano fuori scope per tutte e tre le Story della sequenza:
  1. `ManageableRequestsPage.tsx` e `ManageableRequestDetail.tsx` (volontario, "richieste gestibili"): leggono `pub.publicStatus`/`request.publicStatus` da `publicDeviceRequests`, campo che dopo EA-149 non viene piu' scritto — sempre `undefined`/assente per ogni richiesta nuova o cambiata di stato dopo il deploy di EA-149. Inoltre `ManageableRequestsPage.tsx:117` filtra `deviceRequests` con `where("status", "in", PUBLIC_STATUS_GROUPS["da gestire"])` usando il **vecchio** gruppo a 19 valori (`validata`, `famiglia contattata`, `definizione richiesta`, `valutazione fattibilità`, `attesa volontario`): dopo EA-148 i tre stati granulari collassano nel valore letterale `"da gestire"`, che non compare in quella lista — le richieste con `status === "da gestire"` non vengono piu' trovate da questa query, un sottoinsieme silenzioso della lista "richieste gestibili" per il volontario.
  2. `VolunteerRequestDetail.tsx`: legge `publicData.publicStatus` (sempre assente dopo EA-149) e lo mostra in un `Badge`, che di conseguenza sparisce silenziosamente (il rendering è condizionato su `request.publicStatus &&`) per ogni richiesta toccata dopo il deploy di EA-149.
  3. `AdminMaintenanceRequests.tsx` (tool di manutenzione import CSV, non un consumer di visualizzazione): calcola `publicData.publicStatus` con `mapInternalStatusToPublic` (vecchio helper, dominio a 19 valori, non toccato da EA-150 perché e' l'unico consumer che lo usa ancora) solo per la preview "Dry Run"/"Parsed Data" — il valore non viene mai effettivamente scritto su Firestore (il flusso reale `handleImport` non lo referenzia). Nessun impatto funzionale, ma se `mapInternalStatusToPublic`/`PUBLIC_STATUS_GROUPS` venissero rimossi in una Story futura (sostituiti per intero da `getPublicStatusGroup`/`PUBLIC_STATUS_GROUPS_FROM_STATUS` introdotti da EA-150), questo e' l'unico import residuo da aggiornare prima di poter cancellare il vecchio codice.
  - Questi 4 file si aggiungono a [[F-29]] (già aperto, sullo stesso tema per `REQUEST_STATUSES`/`deviceStatus`): entrambi i finding descrivono superfici di codice che la sequenza EA-148/EA-149/EA-150 ha lasciato, per perimetro dichiarato esplicito di ciascuna Story, fuori dalla migrazione al nuovo dominio a 11 valori.
- **Evidenza**:
  - `enable-device/src/pages/volunteer/ManageableRequestsPage.tsx:117` — `where("status", "in", PUBLIC_STATUS_GROUPS["da gestire"])`, dominio pre-EA-148
  - `enable-device/src/pages/volunteer/ManageableRequestsPage.tsx:139` — `publicStatus: pub.publicStatus ?? null`
  - `enable-device/src/pages/volunteer/ManageableRequestDetail.tsx:145-160` — rendering di `request.publicStatus`
  - `enable-device/src/pages/volunteer/VolunteerRequestDetail.tsx:64,142-145` — lettura e rendering condizionale di `publicData.publicStatus`
  - `enable-device/src/pages/admin/requests/AdminMaintenanceRequests.tsx:14,129,157-159,209,230` — uso di `mapInternalStatusToPublic`, valore mai scritto su Firestore
- **Story/PR/sessione di provenienza**: rilevato durante l'implementazione della Story Jira EA-150, sessione 2026-08-07, verificando tutti i riferimenti a `publicStatus`/`PUBLIC_STATUS_GROUPS` nel codebase oltre ai 7 file esplicitamente nel perimetro della Story.
- **Stato**: risolto (fix diretto, 2026-08-12, su richiesta esplicita dell'operatore). Tutti e 4 i file migrati a `getPublicStatusGroup`/`PUBLIC_STATUS_GROUPS_FROM_STATUS`: `ManageableRequestsPage.tsx` corregge la query "da gestire" (ora include il letterale `"da gestire"`, prima sotto-contante) e rimuove il campo morto `publicStatus` mai renderizzato; `ManageableRequestDetail.tsx`/`VolunteerRequestDetail.tsx` derivano `publicStatus` da `status` invece di leggerlo dal documento pubblico (mai scritto da EA-149); `AdminMaintenanceRequests.tsx` (preview-only, nessuna scrittura reale) normalizza prima con `REMOVED_STATUS_TO_GENERIC` per restare coerente con lo stato post-migrazione. `PUBLIC_STATUS_GROUPS`/`mapInternalStatusToPublic` (dominio a 19 valori) rimossi per intero da `requestStatus.ts`, essendo rimasti senza consumer.

## F-32: EA-151 introduce un flag `deviceRequests.productionChecklistCreated` non richiesto esplicitamente dalla Story per soddisfare lo Scenario 2 (nessuna duplicazione della checklist di produzione), in assenza di un marcatore che identifichi quale checklist collegata sia "quella di produzione"

- **Descrizione**: la Story EA-151 richiede che una seconda transizione a `in produzione` (es. dopo standby) non crei una seconda checklist di produzione, ma esclude esplicitamente dallo scope l'introduzione di un marcatore `purpose`/`production` che identifichi quale, tra le fino a 5 checklist collegabili via `checklistIds`, rappresenti la produzione ("la scelta di quale checklist collegata rappresenti la produzione resta a chi gestisce la richiesta, non è automatizzata"). Senza un simile marcatore, non c'è modo di ispezionare `checklistIds` per capire se una checklist di produzione è già stata auto-creata: un `deviceRequest` potrebbe avere checklist manuali preesistenti scollegate dalla produzione, quindi `checklistIds.length > 0` non è un proxy valido di idempotenza. La sessione ha risolto l'ambiguità aggiungendo un booleano dedicato e indipendente, `deviceRequests/{id}.productionChecklistCreated` (valorizzato da `device-requests/autoCreateProductionChecklist.ts` solo dopo la prima auto-creazione riuscita, mai da un consumer esterno), che non referenzia alcun `checklistId` e quindi non introduce il marcatore esplicitamente escluso dallo scope — ma resta comunque un nuovo campo persistito sul documento `deviceRequests`, non menzionato né dalla descrizione né dagli scenari Gherkin della Story.
- **Evidenza**:
  - Descrizione Story Jira EA-151, sezione "Fuori scope": "Nessun marcatore per identificare 'la' checklist di produzione tra le fino a 5 collegabili (decisione esplicita del supervisor: non introdotto)"
  - `functions/backend/src/device-requests/autoCreateProductionChecklist.ts` — guard `productionChecklistCreated`, letto e "claimato" (compare-and-swap) in un'unica transazione Firestore interna al modulo stesso, PRIMA di creare la checklist (fix di race condition successivo alla prima versione: due `changeStatus` quasi simultanei leggerebbero altrimenti entrambi il flag `false`)
  - `functions/backend/src/device/changeStatus.ts` — NON legge più `productionChecklistCreated`: delega passando solo `requestId`/`newStatus`, è il modulo di auto-creazione a leggere il flag fresco dentro la propria transazione
- **Story/PR/sessione di provenienza**: rilevato durante l'implementazione della Story Jira EA-151, sessione 2026-08-08.
- **Stato**: preservato as-is. Il flag è additivo, non introduce un marcatore "quale checklist è di produzione" (non contiene un `checklistId`) e non modifica alcun contratto Firestore esistente: se il flag risultasse comunque indesiderato secondo l'intento originale del supervisor, l'alternativa più prossima esplorata durante l'implementazione — dedurre l'idempotenza da una query sugli eventi `status_change` con `toStatus === "in produzione"` già esistenti — avrebbe richiesto una query aggiuntiva ad ogni `changeStatus` senza persistere alcun nuovo campo. Segnalato per verifica umana; nessun Task Jira separato proposto in assenza di un'indicazione che il flag non sia accettabile.

## F-33: la voce di menu che porta a `AdminMaintenanceRequests.tsx` (`/admin/requests/maintenance`) è commentata in `AdminLayout.tsx`, quindi lo strumento di migrazione one-shot di EA-152 è raggiungibile solo digitando l'URL a mano

- **Descrizione**: la Story EA-152 estende `AdminMaintenanceRequests.tsx` (stesso file del pattern "Migra volontari" citato dalla Story) con un nuovo bottone "Migra stati richieste" che esegue la migrazione one-shot dei 10 stati rimossi e la rimozione di `publicStatus`. La pagina che ospita questo bottone è registrata come rotta (`AdminLayout.tsx`, `<Route path="requests/maintenance" element={<AdminMaintenanceRequests />} />`) ma la voce di menu laterale corrispondente è commentata, non rimossa: `// { label: "Manutenzione (Import CSV)", ... command: () => navigate("/admin/requests/maintenance") }`. Un admin che non conosca già l'URL diretto non ha modo di raggiungere la pagina — e quindi il nuovo bottone di migrazione — dalla UI. Non è una regressione introdotta da questa Story: il bottone "Migra volontari" preesistente nello stesso file soffre già dello stesso limite.
- **Evidenza**:
  - `enable-device/src/components/layout/AdminLayout.tsx:236` — voce di menu commentata
  - `enable-device/src/components/layout/AdminLayout.tsx:435` — rotta comunque attiva e funzionante
- **Story/PR/sessione di provenienza**: rilevato durante l'implementazione della Story Jira EA-152, sessione 2026-08-08.
- **Stato**: risolto. L'operatore ha richiesto esplicitamente la riabilitazione (sessione 2026-08-09, per poter eseguire la migrazione EA-152 su staging/prod senza conoscere l'URL a memoria): voce di menu scommentata in `AdminLayout.tsx`, nessun'altra modifica. Lo stesso limite resta invece ancora presente sul bottone preesistente "Migra volontari" nello stesso file (non in scope di questa richiesta).

## F-34: la descrizione della Story Jira EA-154 rimanda a "docs/FINDINGS.md F-34" per spiegare perché dichiara `volunteer-management` in Target Domains, ma nessuna voce F-34 preesisteva in questo file al momento dell'implementazione

- **Descrizione**: la Story EA-154 (Epic EA-153) motiva la propria collocazione file (`enable-device/src/pages/volunteer/**`, dominio `volunteer-management` secondo `domain-manifest.json`) con una nota: "vedi docs/FINDINGS.md F-34 sul perché questa Story dichiara volunteer-management in Target Domains pur non essendo nel campo omonimo dell'Epic padre". Al momento dell'implementazione, l'ultima voce presente in questo file era F-33: nessuna voce F-34 esisteva già, né con quel contenuto né con altro. La motivazione stessa (perimetro file coerente con `domain-manifest.json`, pattern identico alle altre pagine sotto `enable-device/src/pages/volunteer/`) è comunque verificabile direttamente dal manifest e non ha impedito l'implementazione, ma il rimando è un riferimento a vuoto: o la voce era prevista per essere scritta da una sessione precedente (Epic EA-153 o pianificazione) e non è stata salvata, oppure il numero progressivo atteso non coincideva più con F-34 al momento dell'esecuzione di questa Story (altre Story dell'Epic potrebbero aver consumato nel frattempo lo stesso numero per un finding diverso). Questa stessa voce occupa ora il numero F-34, in modo coincidentale ma non verificabile a priori come corretto rispetto all'intento originale di chi ha scritto la Story.
- **Evidenza**:
  - Descrizione Story Jira EA-154, sezione "Contesto": "Nota: vedi docs/FINDINGS.md F-34 sul perché..."
  - `docs/FINDINGS.md`, stato al momento dell'implementazione: ultima voce `F-33` (nessuna `F-34` presente)
  - `docs/cognitive-workspace/domain-manifest.json`, dominio `volunteer-management`: `paths` include `enable-device/src/pages/volunteer/**`, coerente con la collocazione scelta indipendentemente dal rimando mancante
- **Story/PR/sessione di provenienza**: rilevato durante l'implementazione della Story Jira EA-154, sessione 2026-08-09.
- **Stato**: chiuso (deciso dall'operatore, 2026-08-12). Non bloccante per la Story originale (la collocazione file era comunque verificabile da `domain-manifest.json`) e l'Epic EA-153 è ormai completa: nessuna azione residua.

## F-35: `enable-device/src/pages/admin/requests/RequestDetail.tsx` fallisce già `npm run lint` con 27 errori preesistenti (`react-hooks/set-state-in-effect`, `@typescript-eslint/no-explicit-any`), non introdotti da EA-158 ma estesi per coerenza di stile dal codice aggiunto da questa Story

- **Descrizione**: `RequestDetail.tsx` viola già, prima di questa Story, due regole ESLint del progetto su larga scala: `react-hooks/set-state-in-effect` (chiamate a `setState` sincrone dentro `useEffect`, es. il blocco `loadData()`/`fetchVolunteers()` in apertura componente e il reset di `showAssignVolunteerDialog`) e `@typescript-eslint/no-explicit-any` (ogni handler del file usa `catch (err: any)`), per un totale di 27 errori/1 warning verificati sulla revisione precedente a questa Story (`b1c7768`). Il pannello "Liberatorie familiari" aggiunto da questa Story (due `useEffect` per risolvere il nome dell'admin che ha acquisito `waiverAcquiredBy`/`photoReleaseAcquiredBy`, e `handleAcquireConsent` con lo stesso `catch (err: any)` degli altri handler dello stesso file) segue lo stesso pattern già presente ovunque nel file, portando il conteggio a 30 errori/1 warning. Nessun nuovo tipo di violazione: solo più istanze delle stesse due regole già pervasive.
- **Evidenza**:
  - `npx eslint src/pages/admin/requests/RequestDetail.tsx` su `b1c7768` (prima di EA-158): 27 errori, 1 warning
  - Stesso comando dopo EA-158: 30 errori, 1 warning — i 3 nuovi sono 2× `react-hooks/set-state-in-effect` (righe dei due nuovi `useEffect` per `waiverAcquiredByName`/`photoReleaseAcquiredByName`) e 1× `@typescript-eslint/no-explicit-any` (`catch (err: any)` in `handleAcquireConsent`, identico agli altri ~15 handler dello stesso file)
- **Story/PR/sessione di provenienza**: rilevato durante l'implementazione della Story Jira EA-158, sessione 2026-08-12.
- **Stato**: preservato as-is. Non corretto in questa Story: la Story riguarda l'acquisizione delle liberatorie familiari, non una remediation del debito di lint del file (che precede EA-158 e riguarda l'intero componente, non solo il codice aggiunto qui). Segnalato per visibilità; se il team decide di affrontare il debito ESLint di questo file, va trattato come intervento dedicato (probabile refactor più ampio di tutti gli `useEffect` e i tipi `any` del componente), non come parte di una Story di feature additiva.

## F-36: la Story Jira EA-159 dichiara di dipendere dal campo `waiverAcquired` introdotto da EA-158 ("va implementata dopo"), ma il branch di EA-159 è stato creato senza che EA-158 fosse ancora mergiata su `main`

- **Descrizione**: la descrizione della Story EA-159 richiede esplicitamente che il gate legga `deviceRequests/{id}.waiverAcquired`, campo introdotto da `setDeviceRequestConsent.ts` (Story EA-158, branch `ea-158-admin-acquisisce-scarico-di-responsabilit-e-liberatoria-foto`) e dichiara una dipendenza d'ordine ("non introduce campi propri, va implementata dopo"). Al momento dell'implementazione di EA-159, il branch di EA-158 esiste ma non risulta mergiato su `main` (né antenato del branch di EA-159): i due branch divergono dallo stesso commit base (`321928d`). Poiché Firestore è schema-less e il gate qui implementato legge il campo per nome stringa (`requestData?.waiverAcquired`) senza importare alcun simbolo da `setDeviceRequestConsent.ts`, l'implementazione di EA-159 non richiede in pratica che il codice di EA-158 sia già mergiato — ma la dipendenza funzionale end-to-end (un admin può effettivamente valorizzare `waiverAcquired` prima che il gate abbia senso in produzione) resta reale e non verificabile da questa sessione, che non ha accesso allo stato di merge delle PR.
- **Evidenza**:
  - `git log --all --oneline`: branch `ea-158-...` con i commit `4cb5cf9`/`a12c2d9`, non antenati di `HEAD` sul branch `ea-159-...` (`git merge-base --is-ancestor a12c2d9 HEAD` → false)
  - `functions/backend/src/device/setDeviceRequestConsent.ts` (branch EA-158): unico punto che scrive `waiverAcquired`/`waiverAcquiredDate`/`waiverAcquiredBy` su `deviceRequests/{id}`
  - `functions/backend/src/device/changeStatus.ts`: il gate di questa Story legge `waiverAcquired` per nome, nessun import da EA-158
- **Story/PR/sessione di provenienza**: rilevato durante l'implementazione della Story Jira EA-159, sessione 2026-08-12.
- **Stato**: risolto. EA-158 è stata mergiata (PR #91, sha `7f59782`) prima della chiusura di EA-159 — l'ordine di merge EA-158 → EA-159 indicato dalla Story stessa è stato rispettato, il gate ha ora un percorso reale per valorizzare `waiverAcquired` in produzione.
## F-37: la Story/Epic EA-160 dichiara che nessun sender esistente nel codice usa il meccanismo nativo a template dell'estensione Trigger Email, ma `activateVolunteers` lo usa già oggi (oltre a un secondo invio ridondante via `message.html` per la stessa email)

- **Descrizione**: sia `docs/implementation-requests/family-waiver-photo-release-request.md` sia la descrizione della Story Jira EA-160 affermano esplicitamente che nessun sender nel codice usa ancora lo schema nativo `template: { name, data }` sui documenti `mail` (solo `AdminEmailLogsPage.tsx` lo gestisce difensivamente in lettura), presentando quindi EA-160 come "il primo utilizzo nel codice del meccanismo nativo a template dell'estensione". Verificando il codice durante l'implementazione, `activateVolunteers` in `functions/backend/src/volunteer/volunteerState.ts` scrive già oggi, per ogni volontario attivato, **due** documenti in `mail` per la stessa email di conferma: un primo con `message: { subject, html }` inline (schema legacy) e un secondo, subito dopo, con `template: { name: "attivazioneVolontario", data: {} }` (schema nativo a template) — quindi un consumer del meccanismo a template esiste già, e la Story eredita una premessa fattuale non più vera al momento dell'implementazione. Il codice del secondo invio è inoltre lievemente incoerente con le altre convenzioni email del repo: usa `to: email` (stringa) invece di `to: [email]` (array, come tutti gli altri sender in `utils/email.ts`/`autoCreateProductionChecklist.ts`/questa Story) e `Timestamp.now()` esplicito invece di lasciare il campo `createdAt` implicito/`FieldValue.serverTimestamp()`. Non risulta inoltre alcun documento `emailTemplates/attivazioneVolontario` referenziato o creato altrove nel repo: se quel documento non esiste sul progetto Firebase live, il secondo invio fallisce silenziosamente lato estensione (l'errore comparirebbe solo nel campo `delivery` del documento `mail`, mai loggato/gestito lato Cloud Function) senza impedire il primo invio via `message.html`, che copre comunque la conferma.
- **Evidenza**:
  - `docs/implementation-requests/family-waiver-photo-release-request.md`, sezione "Aggiornamento (refinement Jira...)": "nessun sender esistente nel codice lo usa ancora... Questa Epic introduce quindi il primo utilizzo nel codice del meccanismo nativo a template dell'estensione"
  - `functions/backend/src/volunteer/volunteerState.ts:52-77` (funzione `activateVolunteers`) — scrittura `message.html` (righe ~52-64) seguita da una seconda scrittura `template: { name: "attivazioneVolontario", data: {} }` (righe ~68-75) sulla stessa collection `mail`, per la stessa email, nello stesso ciclo
  - `functions/backend/src/volunteer/volunteerState.ts:73` — `to: email` (stringa), incoerente con `to: [email]` usato altrove
  - Nessun risultato per `attivazioneVolontario` in altri file del repo (nessun writer/reference a un documento `emailTemplates` con quel nome)
- **Story/PR/sessione di provenienza**: rilevato durante l'implementazione della Story Jira EA-160, sessione 2026-08-12, verificando i sender esistenti prima di introdurre `sendDocumentsEmail.ts` come "primo utilizzo" dichiarato dalla Story.
- **Stato**: preservato as-is. Non corretto in questa Story: `volunteerState.ts` è fuori dal perimetro dichiarato di EA-160 (che introduce solo `sendDocumentsEmail.ts`), e la correzione (rimuovere il doppio invio o verificare/creare il template `attivazioneVolontario` mancante) richiede una decisione di prodotto su quale dei due invii sia quello voluto. Proposta: Task Jira di manutenzione (Epic non specificato, dominio `volunteer-management`) per (a) verificare se `emailTemplates/attivazioneVolontario` esiste sul progetto Firebase live e, in caso contrario, crearlo o rimuovere il secondo invio ridondante, e (b) allineare lo stile del write (`to` come array, `FieldValue.serverTimestamp()`) alle convenzioni del resto del repo — riferimento F-37.

## F-38: il documento `emailTemplates/{id}` referenziato da `sendDocumentsEmail.ts` (EA-160) non può essere creato da questa sessione: la collection vive solo sul progetto Firebase live, senza infrastruttura di seed/migrazione in questo repo

- **Descrizione**: la Story Jira EA-160 richiede la creazione ex novo di un documento template in `emailTemplates` (collection dell'estensione Firebase "Trigger Email", configurata solo sul progetto live, non tracciata in questo repo — nessun `extensions/*.env`, nessuno script di seed/migrazione Firestore trovato in `functions/backend` o altrove) con contenuto (oggetto della comunicazione, corpo HTML, variabili attese) da definire in fase di implementazione. Questa sessione ha definito l'id del documento e le variabili `data` attese (`recipientName`, `requestNumber`, vedi commento su `DOCUMENTS_EMAIL_TEMPLATE_ID` in `sendDocumentsEmail.ts`), ma non può creare il documento stesso: non esiste alcun meccanismo in questo repo per scrivere dati applicativi (a differenza di codice/regole) sul progetto Firebase live, e l'agente di implementazione non ha accesso di deploy. Finché il documento `emailTemplates/device-request-documents-transmission` non viene creato manualmente sul progetto live, ogni chiamata riuscita a `sendDocumentsEmail` scrive comunque il documento `mail` e imposta `documentsEmailSent: true` (il contratto Firestore/UI della Story è soddisfatto), ma l'estensione Trigger Email non troverà il template referenziato e l'invio effettivo fallirà lato estensione (visibile solo nel campo `delivery` del documento `mail`, coerentemente con come l'estensione segnala già gli errori altrove nel repo, es. `AdminEmailLogsPage.tsx`).
- **Evidenza**:
  - `docs/implementation-requests/family-waiver-photo-release-request.md`: "la configurazione dell'estensione non è tracciata in questo repo, solo sul progetto Firebase live"
  - Ricerca in questa sessione: nessun file `extensions/*.env`, nessuno script `scripts/seed*`/`scripts/migration*` nel repo per popolare collection Firestore applicative
  - `functions/backend/src/device-requests/sendDocumentsEmail.ts` — costante `DOCUMENTS_EMAIL_TEMPLATE_ID = "device-request-documents-transmission"` e commento con le variabili `data` attese dal template
- **Story/PR/sessione di provenienza**: rilevato durante l'implementazione della Story Jira EA-160, sessione 2026-08-12.
- **Stato**: da decidere (richiede azione umana fuori da questo repo). Proposta: Task Jira (Epic EA-157 o equivalente) per creare manualmente, sul progetto Firebase live, il documento `emailTemplates/device-request-documents-transmission` con oggetto/corpo del messaggio (trasmissione formale dello scarico di responsabilità da firmare + documenti accessori/informazioni di sicurezza, come da descrizione Story) e placeholder per `{{recipientName}}`/`{{requestNumber}}`, prima che il command button "Invia documenti" produca un invio effettivo in produzione — riferimento F-38.

